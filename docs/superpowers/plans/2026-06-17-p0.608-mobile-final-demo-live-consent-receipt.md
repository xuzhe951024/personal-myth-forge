# P0.608 Mobile Final Demo Live Consent Receipt Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the iPhone final demo launch receipt decode and display backend live-provider consent metadata from top-level first blocker and next action payloads.

**Architecture:** Keep the backend report shape unchanged. Extend the Swift Codable models with optional metadata fields, then append compact markers in the existing receipt row builders.

**Tech Stack:** SwiftPM mobile core, Codable models, pure Swift contract tests.

---

### Task 1: RED Contract

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [x] **Step 1: Add a failing test**

Add `testFinalLaunchMobileSummaryShowsFinalDemoLaunchLiveConsent` with a top-level
final-demo first blocker and next action containing:

```json
"requires_live_provider_consent": true,
"completion_requires_live_provider_consent": true
```

Assert the Swift report models expose those booleans and the launch receipt rows
include live-provider consent markers.

- [x] **Step 2: Verify RED**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: FAIL because `FinalDemoLaunchFirstBlocker` and
`FinalDemoLaunchNextAction` have no consent metadata properties.

Observed: compile failed with missing `requiresLiveProviderConsent` and
`completionRequiresLiveProviderConsent` members on both structs.

### Task 2: GREEN Implementation

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [x] **Step 1: Add model fields**

Add optional `requiresCostConsent`, `requiresLiveProviderConsent`, and
`completionRequiresLiveProviderConsent` fields to `FinalDemoLaunchFirstBlocker`
and `FinalDemoLaunchNextAction`.

- [x] **Step 2: Display receipt markers**

Append:

- `cost consent required`
- `live provider consent required`
- `completion live provider consent required`

when the corresponding optional booleans are true.

- [x] **Step 3: Verify GREEN**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: PASS.

Observed: `PersonalMythForgeMobileCoreContractTests passed`.

### Task 3: Handoff Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`
- Create: `docs/superpowers/specs/2026-06-17-p0.608-mobile-final-demo-live-consent-receipt.md`
- Create: `docs/superpowers/plans/2026-06-17-p0.608-mobile-final-demo-live-consent-receipt.md`

- [x] **Step 1: Run source/compile gates**

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios
```

Observed: contract tests, project checks, and Swift package build passed.

- [x] **Step 2: Run backend source gate and hygiene checks**

```bash
cd services/backend
PMF_SKIP_DOTENV=1 uv run pytest tests/test_ios_showcase_acceptance.py -q
cd ../..
git diff --check
```

Observed: `tests/test_ios_showcase_acceptance.py` passed with 42 tests, `git diff --check` passed, and `make backend-lint` passed.

- [ ] **Step 3: Commit, merge, and push**

Commit the branch, merge it back to `main`, and push `origin/main`.
