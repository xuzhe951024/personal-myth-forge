# P0.590 External Ledger Live Device Action Pruning Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Follow TDD and update this checklist as work completes.

**Goal:** Remove device fallback actions from `final-external-action-ledger.operator_actions` when live provider evidence is already the first blocker.

**Architecture:** Keep source action groups intact. Apply pruning only to top-level operator actions after existing configured-live and device pruning logic.

## Tasks

- [x] Read current git state, design docs, roadmap, and saved `.local` readiness reports.
- [x] Identify `final-external-action-ledger` as the remaining live-first handoff noise.
- [x] Add RED coverage to the live-first external ledger test by asserting `backend-device-demo` is absent.
- [x] Confirm RED with the focused test.
- [x] Add `_drop_device_fallbacks_when_live_provider_is_first()` to top-level operator action generation.
- [x] Confirm GREEN with the focused test.
- [x] Run the full `test_final_external_action_ledger.py` regression.
- [x] Run backend lint and diff checks.
- [x] Refresh local handoff/readiness reports and confirm the saved operator action list.
- [x] Merge to `main`, commit, and push.

## Verification Commands

```bash
cd services/backend
uv run pytest tests/test_final_external_action_ledger.py::test_external_action_ledger_live_first_blocker_uses_guarded_operator_action -q
uv run pytest tests/test_final_external_action_ledger.py -q
```

Additional final checks:

```bash
make backend-lint
git diff --check
```
