# P0.237 Mobile Device Action Evidence Detail Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show saved device action evidence details in the iPhone Final Launch summary.

**Architecture:** Keep the backend report unchanged. Update the Swift summary builder so `showcaseDeviceActionRow(_:)` appends `evidenceDetail` when it is present, after the evidence status/validation command and before the general action detail. Protect the behavior with the existing Swift contract test around final showcase device action bundle rendering.

**Tech Stack:** SwiftPM, Swift Codable models, `PersonalMythForgeMobileCore`, existing shell/Make verification.

---

### Task 1: Contract Test

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [ ] **Step 1: Write the failing test**

Update `testFinalLaunchMobileSummaryShowsFinalShowcaseDeviceActionBundle()` to assert the rendered text contains:

```swift
try expectContains(text, "PMF_BACKEND_BASE_URL must be iPhone-reachable")
try expectContains(text, "Missing DEVELOPMENT_TEAM; PMF_BACKEND_BASE_URL must be iPhone-reachable")
```

- [ ] **Step 2: Run test to verify it fails**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: fails because `FinalLaunchMobileSummaryBuilder.showcaseDeviceActionRow(_:)` omits `evidenceDetail`.

### Task 2: Summary Builder

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [ ] **Step 1: Implement minimal row change**

In `showcaseDeviceActionRow(_:)`, append evidence detail when present:

```swift
if let evidenceDetail = action.evidenceDetail, !evidenceDetail.isEmpty {
    parts.append("| \(evidenceDetail)")
}
```

Place this after the evidence status block and before `action.detail`.

- [ ] **Step 2: Run focused Swift contract test**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: passed.

### Task 3: Verification And Handoff

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Add roadmap entry**

Add:

```markdown
- P0.237 iPhone Final Launch summary rows surface device action evidence details
```

- [ ] **Step 2: Run verification**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
cd services/backend && uv run pytest tests/test_final_showcase_readiness.py
make final-local-report-refresh-local
git diff --check
```

Expected:

- Swift contract tests pass.
- Swift project checks pass.
- Focused backend readiness tests pass.
- Final local report refresh writes `.local/final-local-report-refresh.json` and accepts blocked exit 2 while external resources remain missing.
- `git diff --check` exits 0.

### Task 4: Commit

**Files:**
- Stage all P0.237 files.

- [ ] **Step 1: Commit**

Run:

```bash
git add docs/iteration-roadmap.md \
  docs/superpowers/specs/2026-06-11-p0.237-mobile-device-action-evidence-detail-design.md \
  docs/superpowers/plans/2026-06-11-p0.237-mobile-device-action-evidence-detail.md \
  apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift \
  apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift
git commit -m "Show device action evidence details on mobile"
```

Expected: commit succeeds on `codex/p0.237-mobile-device-action-evidence-detail`.
