# P0.587 Configured Bundle Live Fallback Pruning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Keep `configured-live-evidence-bundle.operator_actions` from presenting fallback live-provider commands beside the guarded configured acceptance command.

**Architecture:** Add a post-dedupe preference step in `configured_live_evidence_bundle._operator_actions()`.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_configured_live_evidence_bundle.py`

- [x] Extend the guarded live provider consent test to include child fallback actions.
- [x] Assert provider handoff, configured eval, configured demo, and bare live-provider fallback actions are pruned.
- [x] Confirm the old implementation fails.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/configured_live_evidence_bundle.py`

- [x] Add a guarded live provider consent action detector.
- [x] Add exact fallback roots for configured live-provider commands.
- [x] Filter fallback roots only when the guarded consent command is present.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.587 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused configured bundle tests.
- [x] Run downstream closure packet and external action ledger tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect configured bundle operator actions.

Safe refresh note: `configured-live-evidence-bundle.json.operator_actions` now contains only `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make final-acceptance-configured; rerun make live-provider-evidence` for the live-provider path. Downstream closure and external ledger reports preserve the guarded command plus safe validation/device follow-up actions.
