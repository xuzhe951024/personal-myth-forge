# P0.584 Showcase Provider Key Live Consent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Align `final-showcase-readiness.capabilities.provider_key_handoff` with the guarded live provider evidence handoff used by the 3D and NPC capabilities.

**Architecture:** Reuse `final_showcase_readiness._report_next_action_metadata()` inside `_provider_key_handoff_capability()` for the live-evidence-unproven branch.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_showcase_readiness.py`

- [x] Extend the existing live-provider child action promotion test to include `provider_key_handoff`.
- [x] Confirm the old implementation fails because it emits bare `make live-provider-evidence`.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_showcase_readiness.py`

- [x] In the live-evidence-unproven branch, promote child command and validation metadata.
- [x] Carry `requires_live_provider_consent`.
- [x] Mark completion as requiring live provider consent.
- [x] Keep local resource and configured bundle branches unchanged.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.584 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused final showcase readiness tests.
- [x] Run downstream focused tests if final demo launch output changes.
- [x] Run `make backend-lint` and `git diff --check`.
- [ ] Refresh safe reports and inspect provider handoff actions.
