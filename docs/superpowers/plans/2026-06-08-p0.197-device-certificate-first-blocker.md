# P0.197 Device Certificate First Blocker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Carry the canonical final demo launch first blocker into the iOS device launch certificate.

**Architecture:** Extend `_final_demo_launch_summary(report)` in `ios_device_launch_certificate.py` with a compact `first_blocker` mapping. Since the full certificate is already sanitized before return, reuse existing sanitization and keep the mapping read-only.

**Tech Stack:** Pytest backend tests plus iOS showcase source acceptance.

---

### Task 1: Backend Contract

**Files:**
- Modify: `services/backend/tests/test_ios_device_launch_certificate.py`
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_certificate.py`

- [x] **Step 1: Add red test**

Assert `result.report["final_demo_launch"]["first_blocker"]` preserves the compact blocker generated by `build_final_demo_launch_report` in `test_ios_device_launch_certificate_ready_with_configured_inputs`.

- [x] **Step 2: Run red pytest**

Run:

```bash
cd services/backend && uv run pytest tests/test_ios_device_launch_certificate.py::test_ios_device_launch_certificate_ready_with_configured_inputs -q
```

Expected: fail with missing `first_blocker`.

- [x] **Step 3: Implement summary field**

Add a small `_first_blocker_summary(report)` helper and include `"first_blocker": _first_blocker_summary(report)` in `_final_demo_launch_summary`.

- [x] **Step 4: Run green pytest**

Run the same focused test and then the full certificate test file.

### Task 2: Source Gate

**Files:**
- Modify: `services/backend/src/myth_forge_api/ios_showcase_acceptance.py`
- Modify: `services/backend/tests/test_ios_showcase_acceptance.py`

- [x] **Step 1: Add source requirement**

Require `first_blocker` in `ios_device_launch_certificate.py` and require the certificate test name.

- [x] **Step 2: Run focused acceptance**

Run:

```bash
cd services/backend && uv run pytest tests/test_ios_showcase_acceptance.py::test_ios_showcase_acceptance_default_contract -q
```

Use the nearest actual test name if this contract uses a different function name.

### Task 3: Verification

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_ios_device_launch_certificate.py tests/test_ios_showcase_acceptance.py tests/test_mobile_final_launch_readiness_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Expected: all pass with only the existing Starlette/httpx warning from backend tests.
