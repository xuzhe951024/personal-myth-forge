# P0.436 Final Demo Device Saved Next Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Preserve the final showcase iOS saved next handoff when final demo
launch promotes it into the top-level `device_action_bundle`.

**Architecture:** Reuse the existing final demo launch device action sanitizer,
but include a sanitized recursive copy of `saved_next_action`. This keeps all
downstream top-level final demo launch consumers aligned with the nested final
showcase readiness source.

**Tech Stack:** Python backend report builder, pytest, SwiftPM contract tests.

---

### File Structure

- Modify `services/backend/tests/test_final_demo_launch.py`
  - Add RED regression for top-level `device_action_bundle.first_action.saved_next_action`
    and the matching `run_mobile_deploy_preflight` action row.
- Modify `services/backend/src/myth_forge_api/final_demo_launch.py`
  - Preserve sanitized `saved_next_action` while copying device actions.
- Modify `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
  - Add contract coverage proving the iPhone Final Launch top-level final demo
    device action row renders the saved next action.
- Modify `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`
  - Pin the new Swift contract test name if project checks require it.
- Modify `docs/iteration-roadmap.md`
  - Add the P0.436 roadmap line.

### Task 1: Backend RED

**Files:**
- Modify: `services/backend/tests/test_final_demo_launch.py`

- [ ] Add a failing test after
  `test_final_demo_launch_device_bundle_first_action_uses_showcase_child_action`.
  The test should build a blocked mobile deploy preflight report, read
  `result.report["device_action_bundle"]`, and assert:
  - top-level `first_action.saved_next_action.command` is the full saved
    operator command
  - top-level matching `run_mobile_deploy_preflight` row has the same
    `saved_next_action`
  - nested `final_showcase_readiness.device_action_bundle.first_action.saved_next_action`
    remains present

- [ ] Run the single new pytest and confirm RED with missing `saved_next_action`.

### Task 2: Backend GREEN

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_demo_launch.py`

- [ ] Update `_device_action` so it copies `saved_next_action` with the existing
  `_device_next_action` sanitizer.
- [ ] Run the single new pytest and focused final demo launch tests.

### Task 3: Swift Contract Coverage

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [ ] Add or extend a Final Launch summary contract payload with top-level
  `device_action_bundle.first_action.saved_next_action` and a matching action
  row saved action.
- [ ] Assert the rendered summary includes the saved next command from the
  top-level final demo device bundle.
- [ ] Run Swift contract tests.

### Task 4: Refresh, Verify, Merge

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] Add roadmap entry:
  `P0.436 final demo launch device action bundle preserves saved next handoff`.
- [ ] Run focused and repository verification.
- [ ] Merge back to `main`, refresh safe local reports, commit, push, and remove
  the worktree after success.
