# P0.619 iOS Rehearsal Final Local Refresh

## Steps

1. Confirm current reports show final showcase live 3D consent while final-local-report-refresh still reports stale device evidence.
2. Add failing script coverage for a final-local-report-refresh run after final-demo launch sync.
3. Update `write_ios_device_launch_rehearsal.sh` to run the safe local refresh at the end.
4. Run focused script tests, lint, and backend regression gates.
5. Refresh safe local evidence, merge to `main`, and push.

## Verification

- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_rehearsal_scripts.py::<focused-test>`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_rehearsal_scripts.py`
- `make backend-lint`
- `make backend-test`
