# P0.461 Functional Regression Fixture Alignment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restore a clean backend functional regression suite by aligning stale test fixtures with current readiness gates.

**Architecture:** Keep production code unchanged. Update focused tests and fixtures in the affected suites so they assert current normalized actions and provide current visual evidence where required.

**Tech Stack:** Python 3.10, pytest, FastAPI backend report builders.

---

### Task 1: Capture RED Baseline

**Files:**
- Test: `services/backend/tests/test_configured_live_evidence_bundle.py`
- Test: `services/backend/tests/test_final_demo_launch.py`
- Test: `services/backend/tests/test_final_handoff_index.py`
- Test: `services/backend/tests/test_ios_device_launch_rehearsal.py`
- Test: `services/backend/tests/test_operator_actions.py`
- Test: `services/backend/tests/test_print_fulfillment_readiness.py`

- [ ] **Step 1: Run focused failing group**

Run the focused pytest command for the 12 failing tests from `make backend-test`.

Expected: FAIL on `main` and on the feature branch before fixture alignment.

### Task 2: Update Configured Preflight Action Expectations

**Files:**
- Modify: `services/backend/tests/test_configured_live_evidence_bundle.py`
- Modify: `services/backend/tests/test_final_handoff_index.py`
- Modify: `services/backend/tests/test_ios_device_launch_rehearsal.py`
- Modify: `services/backend/tests/test_operator_actions.py`

- [ ] **Step 1: Replace stale expected action strings**

Where normalized operator actions are asserted, replace `make final-configured-preflight` expectations with:

```python
"make final-configured-preflight; rerun make configured-live-evidence-bundle"
```

- [ ] **Step 2: Run focused action tests**

Run the affected action normalization tests.

Expected: PASS.

### Task 3: Update Visual And Print Fixtures

**Files:**
- Modify: `services/backend/tests/test_final_demo_launch.py`
- Modify: `services/backend/tests/test_print_fulfillment_readiness.py`

- [ ] **Step 1: Use current visual artifact inventory**

Import `DEFAULT_VISUAL_ARTIFACTS` in each test file that builds a ready visual regression fixture and generate one passed artifact row per default artifact.

- [ ] **Step 2: Preserve print-specific receipt coverage**

Ensure the generated visual fixture includes the print fulfillment receipt artifact expected by `visual_print_receipt`.

- [ ] **Step 3: Run focused visual and print tests**

Run the failing visual and print readiness tests.

Expected: PASS.

### Task 4: Verify Regression Gate

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Add roadmap entry**

Add:

```markdown
- P0.461 Functional regression fixtures align with current readiness gates
```

- [ ] **Step 2: Run verification**

Run:

```bash
cd services/backend && uv run pytest tests/test_configured_live_evidence_bundle.py::test_configured_live_evidence_bundle_blocks_missing_resources_without_running_commands tests/test_final_demo_launch.py::test_configured_final_demo_launch_blocks_missing_resources tests/test_final_demo_launch.py::test_final_demo_launch_embeds_visual_regression_readiness tests/test_final_handoff_index.py::test_final_handoff_index_blocks_missing_reports_without_leaks tests/test_final_handoff_index.py::test_final_handoff_index_promotes_provider_and_print_handoff_from_final_demo tests/test_ios_device_launch_rehearsal.py::test_ios_device_launch_rehearsal_routes_handoff_and_certificate_actions tests/test_ios_device_launch_rehearsal.py::test_ios_device_launch_rehearsal_readiness_preserves_bounded_operator_actions tests/test_ios_device_launch_rehearsal.py::test_ios_device_launch_rehearsal_readiness_normalizes_legacy_copy_actions tests/test_operator_actions.py::test_normalizes_final_handoff_make_wrappers_to_make_targets tests/test_print_fulfillment_readiness.py::test_print_fulfillment_readiness_marks_local_proof_partial_until_treatstock_quote tests/test_print_fulfillment_readiness.py::test_print_fulfillment_readiness_ready_with_configured_treatstock_quote tests/test_print_fulfillment_readiness.py::test_print_fulfillment_readiness_blocks_unsafe_or_unapproved_treatstock_quote -q
cd ../..
make backend-test
make backend-lint
git diff --check
```

Expected: all commands exit 0.
