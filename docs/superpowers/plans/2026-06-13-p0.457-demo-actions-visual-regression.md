# P0.457 Demo Actions Visual Regression Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add default visual regression coverage for the Demo Script external-actions checkpoint and `Check Actions` autopilot state.

**Architecture:** Extend the existing `DEFAULT_VISUAL_ARTIFACTS` tuple and checked-in static verification assets. The visual regression runner already validates HTML existence, PNG dimensions, required text, notes, and unsafe text redaction.

**Tech Stack:** Python 3.10, pytest, static HTML/CSS verification artifacts, PNG screenshot artifact.

---

### Task 1: Add Failing Visual Regression Expectations

**Files:**
- Modify: `services/backend/tests/test_visual_regression.py`

- [ ] **Step 1: Update default artifact id list**

Insert `"p0.457_demo_script_external_actions"` after `"p0.92_demo_script_npc_readiness"` if that artifact is in the default list, otherwise after `"p0.161_final_showcase_provider_handoff_digest"` at the end of the list.

- [ ] **Step 2: Add required text assertions**

Assert the joined `required_text` includes:

```python
assert "External Actions" in required_text
assert "Check Actions" in required_text
assert "backend-device-demo" in required_text
assert "Review external action blockers" in required_text
```

- [ ] **Step 3: Update default summary expectation**

Change the default checked-in artifact summary from:

```python
assert result.report["summary"] == {"passed": 30, "failed": 0}
```

to:

```python
assert result.report["summary"] == {"passed": 31, "failed": 0}
```

- [ ] **Step 4: Run focused test and confirm RED**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py::test_default_visual_artifacts_cover_full_showcase_flow tests/test_visual_regression.py::test_visual_regression_default_passes_checked_in_showcase_artifacts -q
```

Expected: FAIL because the new artifact spec and checked-in files are not present.

### Task 2: Add Visual Artifact Spec and Static Assets

**Files:**
- Modify: `services/backend/src/myth_forge_api/visual_regression.py`
- Create: `docs/superpowers/verification/p0.457-demo-script-external-actions.html`
- Create: `docs/superpowers/verification/assets/p0.457-demo-script-external-actions-390x844.png`
- Create: `docs/superpowers/verification/2026-06-13-p0.457-demo-script-external-actions-regression.md`

- [ ] **Step 1: Add `VisualArtifactSpec`**

Append this spec near the end of `DEFAULT_VISUAL_ARTIFACTS`:

```python
VisualArtifactSpec(
    id="p0.457_demo_script_external_actions",
    html_path="docs/superpowers/verification/p0.457-demo-script-external-actions.html",
    png_path=(
        "docs/superpowers/verification/assets/"
        "p0.457-demo-script-external-actions-390x844.png"
    ),
    notes_path=(
        "docs/superpowers/verification/"
        "2026-06-13-p0.457-demo-script-external-actions-regression.md"
    ),
    width=390,
    height=844,
    required_text=(
        "Demo Script",
        "External Actions",
        "Check Actions",
        "backend-device-demo",
        "Review external action blockers",
        "Final Launch",
        "No providers, Xcode, signing, payment, or global mutation.",
    ),
)
```

- [ ] **Step 2: Create HTML and notes**

Create a 390px mobile static HTML that uses restrained, compact UI and includes all required text visibly. Create notes explaining that the artifact is static, local-only, and checks the Demo Script external-actions checkpoint.

- [ ] **Step 3: Generate PNG**

Render the HTML at 390x844 into `docs/superpowers/verification/assets/p0.457-demo-script-external-actions-390x844.png` using the available local browser/screenshot workflow. Do not use external URLs or live providers.

- [ ] **Step 4: Run visual regression GREEN**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py -q
```

Expected: `10 passed` with the existing Starlette/httpx deprecation warning.

### Task 3: Roadmap and Final Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Append roadmap entry**

Append:

```markdown
- P0.457 Visual regression covers Demo Script external actions handoff
```

- [ ] **Step 2: Run final checks**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py -q
git diff --check
```

Expected: pytest exits 0 and `git diff --check` has no output.
