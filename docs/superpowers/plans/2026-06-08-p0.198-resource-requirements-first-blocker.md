# P0.198 Resource Requirements First Blocker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Add a structured first blocker to final resource requirements so upstream final launch reports can reuse it.

**Architecture:** Compute `first_blocker` from the same requirement rows used for `requirements_by_id` and `operator_actions`. The blocker is a compact metadata object and is sanitized by construction because secret values are never included.

**Tech Stack:** Pytest backend tests plus iOS showcase source acceptance.

---

### Task 1: Backend Contract

**Files:**
- Modify: `services/backend/tests/test_final_resource_requirements.py`
- Modify: `services/backend/src/myth_forge_api/final_resource_requirements.py`

- [x] **Step 1: Add red tests**

Assert:
- missing default requirements expose `first_blocker.id == "MESHY_API_KEY"`
- ready requirements expose `first_blocker is None`
- Treatstock selection exposes `first_blocker.id == "TREATSTOCK_API_KEY"`

- [x] **Step 2: Run red pytest**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_resource_requirements.py -q
```

Expected: fail because `first_blocker` is missing.

- [x] **Step 3: Implement blocker helper**

Add `_first_blocker(requirements)` that returns the first required missing/blocked row with command and detail from existing metadata.

- [x] **Step 4: Run green pytest**

Run the same test file.

### Task 2: Source Gate

**Files:**
- Modify: `services/backend/src/myth_forge_api/ios_showcase_acceptance.py`
- Modify: `services/backend/tests/test_ios_showcase_acceptance.py`

- [x] **Step 1: Require `first_blocker` source**

Add a source requirement for `first_blocker` in `final_resource_requirements.py` and include it in the complete fixture.

- [x] **Step 2: Run focused acceptance**

Run:

```bash
cd services/backend && uv run pytest tests/test_ios_showcase_acceptance.py::test_ios_showcase_acceptance_passes_current_repo_source_gates -q
```

Expected: pass.

### Task 3: Verification

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_final_resource_requirements.py tests/test_ios_showcase_acceptance.py tests/test_final_demo_launch.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Expected: all pass with only the existing Starlette/httpx warning where applicable.
