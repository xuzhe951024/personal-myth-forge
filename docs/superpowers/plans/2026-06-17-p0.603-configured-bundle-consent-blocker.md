# P0.603 Configured Bundle Consent Blocker

## Goal

Make configured live evidence bundle next actions match the configured plan consent
order once local iPhone/backend blockers are clear.

## Steps

1. Refresh current local evidence with backend-device-demo and confirm device preflight
   is ready without live provider calls.
2. Add a RED test showing the bundle current blocker still points at configured final
   acceptance while operator actions start at configured 3D evaluation.
3. Prefer configured plan blockers when plan status is `blocked` or
   `consent_required`.
4. Preserve validation commands and live consent metadata on the surfaced blocker.
5. Run focused tests, lint, safe refresh, commit, merge, and push.

## Safety

This phase only changes read-only report prioritization and local evidence refreshes.
It must not run Meshy/OpenAI calls, write secrets, place print orders, or touch Xcode
signing.
