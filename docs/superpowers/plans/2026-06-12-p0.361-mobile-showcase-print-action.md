# P0.361 Mobile Final Showcase Print Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show the final showcase print fulfillment handoff action in the iPhone Final Launch summary.

**Architecture:** Keep the change in the mobile summary builder. The backend already emits sanitized operator actions; the iPhone only filters, orders, and renders read-only text.

**Tech Stack:** SwiftPM iOS mobile core, Swift contract tests, Python backend source gate.

---

### Task 1: Add RED Mobile Contract Coverage

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [ ] **Step 1: Extend priority action fixture**

In `testFinalLaunchMobileSummaryShowsPriorityFinalShowcaseActions`, add this
action before the hidden extra action:

```swift
"after explicit Treatstock cost consent, save a sanitized services/backend/.local/print-quote-configured.json from POST /v1/print-quotes; rerun make print-fulfillment-readiness",
```

Add:

```swift
try expectContains(text, "after explicit Treatstock cost consent")
try expectContains(text, "make print-fulfillment-readiness")
try expectEqual(summary.showcaseReadinessRows.count, 15)
```

- [ ] **Step 2: Run the focused mobile contract product and confirm RED**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: FAIL because the print handoff action is not selected by
`showcaseReadinessActionRows`.

### Task 2: Implement Priority Selection

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [ ] **Step 1: Add print priority fragments**

Add the print markers to `priorityFragments`:

```swift
"treatstock",
"print-fulfillment",
"print_fulfillment",
```

- [ ] **Step 2: Raise bounded action rows by one**

Change:

```swift
return deduped(selected).prefix(6).map(sanitize)
```

to:

```swift
return deduped(selected).prefix(7).map(sanitize)
```

- [ ] **Step 3: Run the mobile contract product and confirm GREEN**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: PASS.

### Task 3: Verify Source Gate

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Add roadmap entry**

Append:

```markdown
- P0.361 iPhone Final Launch summary prioritizes final showcase print handoff action
```

- [ ] **Step 2: Run backend source acceptance for iOS showcase**

Run:

```bash
cd services/backend
uv run pytest tests/test_ios_showcase_acceptance.py -q
```

Expected: PASS.

- [ ] **Step 3: Run formatting and diff checks**

Run:

```bash
git diff --check
```

Expected: exit 0.
