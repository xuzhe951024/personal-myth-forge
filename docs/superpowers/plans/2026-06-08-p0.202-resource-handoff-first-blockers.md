# P0.202 Resource Handoff First Blockers Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Add structured first blockers to final resource fill guide and apply preview, then expose them on the iPhone final launch panel.

**Architecture:** Backend reports emit sanitized `first_blocker`; final demo launch embeds those reports unchanged; iOS decodes optional first blocker structs and summary rows prefer them over derived list rows. Source acceptance gates protect the contract.

---

### Task 1: Backend Reports

**Files:**
- Modify: `services/backend/tests/test_final_resource_fill_guide.py`
- Modify: `services/backend/src/myth_forge_api/final_resource_fill_guide.py`
- Modify: `services/backend/tests/test_final_resource_apply_preview.py`
- Modify: `services/backend/src/myth_forge_api/final_resource_apply_preview.py`

- [x] **Step 1: Add red backend assertions**

Assert blocked fill guide and apply preview reports include `first_blocker`, and ready reports return `None`.

- [x] **Step 2: Run backend red tests**

Run focused pytest for both files. Expected: fail because `first_blocker` is missing.

- [x] **Step 3: Implement backend first blockers**

Add helper builders using existing sanitized report data. Do not add writes or live calls.

- [x] **Step 4: Run backend green tests**

Run both report test files.

### Task 2: iOS Contract

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [x] **Step 1: Add red Swift contract assertions**

Assert final launch payload decodes fill guide/apply preview first blockers and summary rows include the first blocker commands.

- [x] **Step 2: Run Swift red contract**

Run `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`. Expected: fail at missing model fields.

- [x] **Step 3: Implement iOS decode and summary rows**

Add optional first blocker structs/fields and first-blocker row renderers.

- [x] **Step 4: Run Swift green contract and ProjectChecks**

Run CoreContractTests and ProjectChecks.

### Task 3: Acceptance Gates

**Files:**
- Modify: `services/backend/tests/test_ios_showcase_acceptance.py`
- Modify: `services/backend/src/myth_forge_api/ios_showcase_acceptance.py`
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`

- [x] **Step 1: Add red source gate assertions**

Require backend `first_blocker`, Swift model fields, summary row builders, and contract fixture strings.

- [x] **Step 2: Implement source requirements and fixtures**

Add the missing source requirements and complete fixtures.

### Task 4: Verification

- [x] **Step 1: Run full worktree verification**

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_final_resource_fill_guide.py tests/test_final_resource_apply_preview.py tests/test_final_demo_launch.py tests/test_ios_showcase_acceptance.py tests/test_mobile_final_launch_readiness_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Observed: all commands passed. Backend pytest kept the existing Starlette/httpx deprecation warning. `final-demo-launch-local.json` includes `final_resource_fill_guide.first_blocker` and `final_resource_apply_preview.first_blocker`; configured acceptance was dry-run only.
