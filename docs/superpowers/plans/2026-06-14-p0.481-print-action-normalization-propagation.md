# Print Action Normalization Propagation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove stale prose-only Treatstock quote handoff actions from final aggregation surfaces by preferring the guarded `PMF_ALLOW_PRINT_PROVIDER_CALLS=1 make print-quote-configured` action.

**Architecture:** Add one shared operator action helper that normalizes/dedupes configured print quote handoff roots after existing `normalize_operator_action()` calls. Wire the helper into final demo launch, local refresh, final handoff, iOS launch certificate, rehearsal readiness, and rehearsal aggregators.

**Tech Stack:** Python FastAPI backend reports, pytest, existing Superpowers report specs and Make safe wrappers.

---

### Task 1: Shared Normalizer Helper

**Files:**
- Modify: `services/backend/src/myth_forge_api/operator_actions.py`
- Test: `services/backend/tests/test_operator_actions.py`

- [x] **Step 1: Write the failing test**

Add a test proving a list with both legacy and guarded configured print quote actions keeps only the guarded action.

- [x] **Step 2: Run RED**

Run: `uv run pytest tests/test_operator_actions.py::test_prefers_guarded_print_quote_action_over_legacy_variants -q`

Expected: FAIL with missing `prefer_guarded_print_quote_handoff_actions`.

- [x] **Step 3: Implement helper**

Add `prefer_guarded_print_quote_handoff_actions(actions: list[str]) -> list[str]`.

- [x] **Step 4: Run GREEN**

Run the same focused pytest command and confirm it passes.

### Task 2: Aggregator Propagation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_demo_launch.py`
- Modify: `services/backend/src/myth_forge_api/final_local_report_refresh.py`
- Modify: `services/backend/src/myth_forge_api/final_handoff_index.py`
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_certificate.py`
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_rehearsal.py`
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_rehearsal_readiness.py`
- Test: existing report tests in `services/backend/tests/`

- [x] **Step 1: Write failing report tests**

Add focused tests that feed stale legacy print handoff actions and assert top-level `operator_actions` expose only the guarded target.

- [x] **Step 2: Run RED**

Run the focused tests and confirm they fail because stale legacy actions remain.

- [x] **Step 3: Wire helper into aggregators**

Apply `prefer_guarded_print_quote_handoff_actions()` after existing dedupe/validation normalization and before priority slicing.

- [x] **Step 4: Run GREEN**

Run the focused tests and confirm they pass.

### Task 3: Verification and Handoff

**Files:**
- Modify: `README.md` or `docs/iteration-roadmap.md` only if wording changes are needed.

- [x] **Step 1: Run focused backend tests**

Run operator action and final report tests that cover this handoff path.

- [x] **Step 2: Run lint and Swift source checks**

Run `make backend-lint`, `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`, and `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`.

- [x] **Step 3: Refresh safe local evidence**

Run safe report refresh targets only; do not set live provider consent env vars.

- [ ] **Step 4: Commit, merge, push**

Fast-forward merge into `main`, push `origin/main`, and report remaining final blockers.
