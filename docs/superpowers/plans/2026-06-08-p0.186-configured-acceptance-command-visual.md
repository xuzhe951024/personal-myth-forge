# P0.186 Configured Acceptance Command Visual Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a default visual regression artifact proving the iPhone Final Launch `Commands` rows show `make final-acceptance-configured` for configured mode.

**Architecture:** Extend the existing `DEFAULT_VISUAL_ARTIFACTS` manifest with one static 390x844 proof. The backend visual regression checker already validates HTML existence, PNG existence, dimensions, required text, and unsafe text; the new work only adds a manifest entry, checked-in HTML, checked-in PNG, notes, and tests.

**Tech Stack:** Python visual regression manifest/tests, static HTML, 390x844 PNG screenshot, existing `make visual-regression-local`.

---

### Task 1: Add Failing Visual Manifest Test

**Files:**
- Modify: `services/backend/tests/test_visual_regression.py`

- [x] **Step 1: Add expected artifact id**

Insert `p0.186_configured_acceptance_command_visual` after `p0.153_configured_evidence_visual` in the expected default artifact id list:

```python
"p0.186_configured_acceptance_command_visual",
```

- [x] **Step 2: Add required text assertions**

Add these assertions to `test_default_visual_artifacts_cover_full_showcase_flow`:

```python
assert "make final-acceptance-configured" in required_text
assert "Live providers: consent required for configured acceptance." in required_text
assert "--allow-live-provider-calls" in required_text
assert "commands_run=false live_calls=false global=false" in required_text
```

- [x] **Step 3: Run red test**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py::test_default_visual_artifacts_cover_full_showcase_flow -q
```

Expected: FAIL because `DEFAULT_VISUAL_ARTIFACTS` does not yet include the P0.186 artifact id.

### Task 2: Add Visual Artifact Manifest Entry

**Files:**
- Modify: `services/backend/src/myth_forge_api/visual_regression.py`

- [x] **Step 1: Add default artifact spec**

Insert after `p0.153_configured_evidence_visual`:

```python
VisualArtifactSpec(
    id="p0.186_configured_acceptance_command_visual",
    html_path=(
        "docs/superpowers/verification/"
        "p0.186-configured-acceptance-command-visual.html"
    ),
    png_path=(
        "docs/superpowers/verification/assets/"
        "p0.186-configured-acceptance-command-visual-390x844.png"
    ),
    notes_path=(
        "docs/superpowers/verification/"
        "2026-06-08-p0.186-configured-acceptance-command-visual-regression.md"
    ),
    width=390,
    height=844,
    required_text=(
        "Final Launch Status",
        "Commands",
        "make final-acceptance-configured",
        "Live providers: consent required for configured acceptance.",
        "--allow-live-provider-calls",
        "commands_run=false live_calls=false global=false",
    ),
),
```

- [x] **Step 2: Run red check again**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py::test_visual_regression_default_passes_checked_in_showcase_artifacts -q
```

Expected: FAIL because the HTML and PNG files do not yet exist.

### Task 3: Add Static Visual Proof Files

**Files:**
- Create: `docs/superpowers/verification/p0.186-configured-acceptance-command-visual.html`
- Create: `docs/superpowers/verification/assets/p0.186-configured-acceptance-command-visual-390x844.png`
- Create: `docs/superpowers/verification/2026-06-08-p0.186-configured-acceptance-command-visual-regression.md`

- [x] **Step 1: Add HTML**

Create a 390px-wide static iPhone view with these visible sections:

```text
Final Launch Status
Final launch blocked
Mode
Launch Receipt
Commands
make final-acceptance-configured
make final-resource-requirements
make final-resources-preflight
make final-resource-apply-preview
Live providers: consent required for configured acceptance.
--allow-live-provider-calls
commands_run=false live_calls=false global=false
```

- [x] **Step 2: Render PNG**

Run Chrome headless with a temporary local profile:

```bash
tmp_profile=$(mktemp -d /tmp/pmf-chrome-p0.186.XXXXXX)
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new \
  --disable-gpu \
  --no-first-run \
  --user-data-dir="$tmp_profile" \
  --window-size=390,844 \
  --screenshot=docs/superpowers/verification/assets/p0.186-configured-acceptance-command-visual-390x844.png \
  "file://$PWD/docs/superpowers/verification/p0.186-configured-acceptance-command-visual.html"
rm -rf "$tmp_profile"
```

Expected: PNG is `390 x 844`.

- [x] **Step 3: Add notes**

Document that the artifact is static, read-only, and executes no provider, Xcode, signing, backend env, iOS config, keychain, payment, or global mutation commands.

- [x] **Step 4: Run green visual tests**

Run:

```bash
cd services/backend && uv run pytest tests/test_visual_regression.py -q
```

Expected: PASS with the default visual artifact count increased by one.

### Task 4: Verify And Integrate

**Files:**
- No further source edits expected.

- [x] **Step 1: Run focused and full verification**

Run:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Expected: all checks pass; configured acceptance remains dry-run only.

- [x] **Step 2: Commit, merge, cleanup**

Run:

```bash
git status --short
git add services/backend/src/myth_forge_api/visual_regression.py services/backend/tests/test_visual_regression.py docs/superpowers/verification/p0.186-configured-acceptance-command-visual.html docs/superpowers/verification/assets/p0.186-configured-acceptance-command-visual-390x844.png docs/superpowers/verification/2026-06-08-p0.186-configured-acceptance-command-visual-regression.md docs/superpowers/specs/2026-06-08-p0.186-configured-acceptance-command-visual-design.md docs/superpowers/plans/2026-06-08-p0.186-configured-acceptance-command-visual.md
git commit -m "Add configured acceptance command visual proof"
git -C /Users/zhexu/Codex/personal-myth-forge merge --no-ff codex/p0.186-configured-acceptance-command-visual
```

Expected: merge succeeds on `main`. Do not push.
