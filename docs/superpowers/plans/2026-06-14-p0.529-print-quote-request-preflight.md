# P0.529 Print Quote Request Preflight Plan

## Goal

Make configured Treatstock quote handoff executable by checking the ignored local request file before telling the operator to run `make print-quote-configured`.

## Steps

1. Add RED tests in `services/backend/tests/test_print_fulfillment_readiness.py`.
   - Missing request file should make `configured_treatstock_quote_request` the first blocker.
   - Valid request file plus missing quote should preserve the existing configured quote partial next action.
2. Implement request-file validation in `services/backend/src/myth_forge_api/print_fulfillment_readiness.py`.
   - Add a constant for `services/backend/.local/print-quote-request-configured.json`.
   - Read and validate JSON with `PrintQuoteRequest.model_validate`.
   - Include a sanitized evidence summary and check row.
3. Keep existing configured quote behavior once the request preflight is ready.
4. Update `docs/iteration-roadmap.md` with P0.529.
5. Verify with focused tests, lint, full backend tests, and refreshed `make print-fulfillment-readiness`/`make final-showcase-readiness` evidence.
