# P0.623 Showcase Demo Fallback Pruning Plan

## Goal

Keep final showcase operator actions focused on the concrete live provider consent command when configured Meshy/OpenAI evidence is blocked.

## Steps

1. Add a focused failing test for `final_showcase_readiness._dedupe_operator_actions`.
2. Update live-provider fallback roots so configured demo-launch commands are pruned only when a live consent gate exists.
3. Run the focused test to confirm GREEN.
4. Run `test_final_showcase_readiness.py`, lint, and backend tests.
5. Refresh safe local readiness evidence and inspect `final-showcase-readiness.operator_actions`.
6. Merge to `main` and push.

## Safety

- Do not run any `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 ...` command.
- Do not call Meshy, OpenAI, Treatstock, or other paid providers.
- Do not modify Xcode signing, Keychain, or global machine state.
