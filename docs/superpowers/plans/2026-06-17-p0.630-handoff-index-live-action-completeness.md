# P0.630 Handoff Index Live Action Completeness Plan

## Steps

1. Add a RED assertion proving `final-handoff-index` should keep both configured 3D
   and NPC granular live evidence consent actions.
2. Extend the live acceptance lane so it carries the full granular live action list,
   while preserving the existing first-action fallback behavior.
3. Run focused tests, related handoff/report tests, lint, full backend tests, and safe
   report refresh commands.
4. Merge the isolated worktree back to `main` and push.
