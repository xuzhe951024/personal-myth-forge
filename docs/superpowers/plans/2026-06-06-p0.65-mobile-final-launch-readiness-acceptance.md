# P0.65 Mobile Final Launch Readiness Acceptance Plan

## Scope

Wire P0.64's mobile Final Launch readiness path into final acceptance as a first-class, read-only gate.

## Steps

1. Add failing backend tests for the new acceptance runner.
2. Add failing final acceptance tests for the new in-process check and updated summary counts.
3. Implement `run_mobile_final_launch_readiness_acceptance`.
4. Wire the runner into `run_final_acceptance`.
5. Update project docs and create visual/regression evidence.
6. Run focused tests, backend lint/test, Swift project checks, Swift contract tests, and quick final acceptance.
7. Merge the isolated worktree back to `main` and rerun key checks.

## Verification Commands

```bash
cd /Users/zhexu/Codex/personal-myth-forge/.worktrees/p0.65-mobile-final-launch-readiness-acceptance
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py tests/test_final_acceptance.py
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
cd services/backend && uv run python -m myth_forge_api.cli final-acceptance --profile quick --provider-mode local --repo-root ../..
```
