P0.480 Configured Print Quote Handoff Plan

1. Baseline and RED tests
   - Add backend tests for `print-quote-configured` CLI consent, sanitized output, and readiness operator action text.
   - Add script/Make tests proving the guard blocks without `PMF_ALLOW_PRINT_PROVIDER_CALLS=1`.
   - Add source acceptance expectations for the new script/Make target.
   - Run focused tests and confirm the expected failures.

2. Implement guarded handoff
   - Add `services/backend/scripts/require_print_provider_consent.sh`.
   - Add a `print-quote-configured` CLI command that reads a `PrintQuoteRequest` JSON file, requires an explicit allow argument, calls the configured print provider, sanitizes the quote, and writes the output.
   - Add `make print-quote-configured`.

3. Wire reports
   - Update `print_fulfillment_readiness` configured quote command/operator action to use the guarded target with the explicit cost-consent environment variable.
   - Update commands/source gates and mobile ProjectChecks expectations where they assert the old prose-only handoff.

4. Verify
   - Run focused backend tests for CLI, print fulfillment readiness, script/Make behavior, and source acceptance.
   - Run the Swift ProjectChecks executable.
   - Refresh local readiness reports through safe wrappers only.
   - Merge to `main`, rerun focused verification on `main`, push, and report the remaining blockers.
