# P0.596 Showcase Capability Live Actions Plan

## Goal

Make final showcase capability rows expose capability-specific configured live
provider actions while preserving the top-level first blocker sequence.

## Steps

1. Read current git state, product docs, roadmap, and saved readiness reports.
2. Add a RED test for the NPC row preferring `backend-evaluate-npc-configured`.
3. Add a report helper that selects guarded configured plan operator actions by
   make target.
4. Run focused final showcase tests, lint, and safe local readiness refresh.
5. Commit, merge, and push.

## Safety

This phase only changes report command selection. It must not run any command with
`PMF_ALLOW_LIVE_PROVIDER_CALLS=1`, call live providers, write secrets, submit print
orders, or perform Xcode signing/global Mac changes.
