# P0.187 Visual Index Scope Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make top-level iOS showcase/source acceptance require the P0.186 configured acceptance command visual proof and current 20-artifact README scope.

**Architecture:** Extend existing source-string gates in `test_ios_showcase_acceptance.py`, `ios_showcase_acceptance.py`, and `PersonalMythForgeMobileProjectChecks`. Update README copy to match the current `DEFAULT_VISUAL_ARTIFACTS` count and new configured-command proof.

**Tech Stack:** Python pytest source acceptance, SwiftPM ProjectChecks, Markdown docs, existing visual regression manifest.

---

### Task 1: Add Failing Top-Level Visual Scope Tests

**Files:**
- Modify: `services/backend/tests/test_ios_showcase_acceptance.py`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [x] **Step 1: Add fixture/source expectations to Python acceptance tests**

In `test_ios_showcase_acceptance_passes_complete_fixture`, the complete fixture should need README/source text for:

```python
"20 static 390x844 iPhone evidence artifacts"
"configured acceptance command visual"
"p0.186_configured_acceptance_command_visual"
```

Add these strings to the relevant fixture source requirements for `showcase_visual_regression_index`.

- [x] **Step 2: Add Swift ProjectChecks expectations**

Add ProjectChecks `requireContains` checks so the checked-out repo must include:

```swift
try requireContains(readme, "20 static 390x844 iPhone evidence artifacts", "visual regression artifact count README copy")
try requireContains(readme, "configured acceptance command visual", "configured acceptance visual README copy")
try requireContains(visualRegression, "p0.186_configured_acceptance_command_visual", "configured acceptance command visual artifact")
```

- [x] **Step 3: Run red tests**

Run:

```bash
cd services/backend && uv run pytest tests/test_ios_showcase_acceptance.py::test_ios_showcase_acceptance_passes_complete_fixture -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Expected: both fail because production source gates and README copy do not yet include the new top-level P0.186 scope strings.

### Task 2: Update Source Gates And README

**Files:**
- Modify: `README.md`
- Modify: `services/backend/src/myth_forge_api/ios_showcase_acceptance.py`
- Modify: `services/backend/tests/test_ios_showcase_acceptance.py`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [x] **Step 1: Update README visual gate scope**

Replace the old ten-artifact visual gate paragraph with copy that says the gate checks 20 static 390x844 iPhone artifacts and includes the configured acceptance command visual proof.

- [x] **Step 2: Update backend source acceptance requirement**

In the `showcase_visual_regression_index` feature requirements, add source requirements for:

```python
SourceRequirement("services/backend/src/myth_forge_api/visual_regression.py", "p0.186_configured_acceptance_command_visual"),
SourceRequirement("README.md", "20 static 390x844 iPhone evidence artifacts"),
SourceRequirement("README.md", "configured acceptance command visual"),
```

- [x] **Step 3: Update fixture writer strings**

Update `write_complete_ios_showcase_fixture` so its fake `README.md` and fake `visual_regression.py` include the same new strings.

- [x] **Step 4: Update Swift ProjectChecks**

Read `README.md` and `visual_regression.py` in ProjectChecks if not already read, then require the same three strings.

### Task 3: Verify And Integrate

**Files:**
- No additional source files expected.

- [x] **Step 1: Run focused tests**

Run:

```bash
cd services/backend && uv run pytest tests/test_ios_showcase_acceptance.py -q
cd services/backend && uv run pytest tests/test_visual_regression.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Expected: all pass; visual regression summary remains 20 passed, 0 failed.

- [x] **Step 2: Run full verification**

Run:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Expected: all pass; configured acceptance remains dry-run only.

- [ ] **Step 3: Commit, merge, cleanup**

Run:

```bash
git add README.md services/backend/src/myth_forge_api/ios_showcase_acceptance.py services/backend/tests/test_ios_showcase_acceptance.py apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift docs/superpowers/specs/2026-06-08-p0.187-visual-index-scope-design.md docs/superpowers/plans/2026-06-08-p0.187-visual-index-scope.md
git commit -m "Align visual index source gates with configured command proof"
git -C /Users/zhexu/Codex/personal-myth-forge merge --no-ff codex/p0.187-visual-index-scope -m "Merge p0.187 visual index scope"
```

Expected: merge succeeds on `main`. Do not push.
