# P0.621 Local Refresh Live Action Pruning Plan

## Goal

Make `final-local-report-refresh` operator actions converge on the most specific guarded live-provider consent command when configured evidence is blocked.

## Steps

1. Add a focused failing test showing that a granular `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make backend-evaluate-3d-configured` action should prune device fallbacks and broad configured acceptance actions.
2. Update live-provider consent detection and action preference helpers in `final_local_report_refresh.py`.
3. Run the focused test to confirm GREEN.
4. Run the adjacent `test_final_local_report_refresh.py` suite, lint, and backend tests.
5. Refresh safe local evidence without live provider calls and verify the saved handoff action list.
6. Merge to `main` and push.

## Safety

- No `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make ...` command will be executed.
- No Xcode signing, Keychain, global config, or paid provider action will be performed.
- Only repo files and ignored `.local` evidence generated by safe Make targets may change.
