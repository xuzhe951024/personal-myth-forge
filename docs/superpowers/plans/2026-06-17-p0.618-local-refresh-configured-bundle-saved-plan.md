# P0.618 Local Refresh Configured Bundle Saved Plan

## Steps

1. Confirm current reports show final showcase live 3D consent while configured bundle source falls back to local-stub.
2. Add failing coverage for local refresh configured bundle saved-plan reuse.
3. Remove the explicit local settings override from the configured bundle refresh step so saved plan reuse applies.
4. Run focused, adjacent, lint, and backend regression gates.
5. Refresh safe local evidence, merge to `main`, and push.

## Verification

- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_local_report_refresh.py::<focused-test>`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_local_report_refresh.py`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_configured_live_evidence_bundle.py`
- `make backend-lint`
- `make backend-test`
