# P0.71 Mobile Final Resources Preflight Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show the backend final resources preflight status inside the iPhone Device Preflight summary.

**Architecture:** Extend existing Swift Codable models and `DevicePreflightSummaryBuilder`. Keep all resource data backend-owned and sanitized; iOS only reads the backend report already fetched through `/v1/final-demo-launch`.

**Tech Stack:** SwiftPM mobile core, Swift contract tests, FastAPI source acceptance, static HTML visual evidence.

---

### Task 1: Swift Model and Summary Contract

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/DevicePreflight.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [x] **Step 1: Write failing Swift contract tests**

Add tests that decode `final_resources_preflight`, assert `final_resources`
summary item states for missing/blocked/ready, and assert unsafe detail
redaction.

- [x] **Step 2: Run red test**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: fail because Swift models and summary item do not exist.

- [x] **Step 3: Implement minimal Swift model and summary row**

Add Codable structs and `finalResourcesItem(report:error:)` logic inside
`DevicePreflightSummaryBuilder`.

- [x] **Step 4: Run green test**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: pass.

### Task 2: Source Acceptance and Docs

**Files:**
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`
- Modify: `README.md`
- Modify: `apps/mobile/README.md`
- Modify: `docs/iteration-roadmap.md`

- [x] **Step 1: Add failing source acceptance requirements**

Require `FinalResourcesPreflightReport`, `finalResourcesPreflight`, and
`final_resources` in mobile source checks.

- [x] **Step 2: Run red focused backend test**

Run:

```bash
cd services/backend
uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
```

Expected: fail until source checks and fixtures are updated.

- [x] **Step 3: Update acceptance fixture and docs**

Update mobile final launch readiness acceptance and docs.

- [x] **Step 4: Run focused backend test**

Run:

```bash
cd services/backend
uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
```

Expected: pass.

### Task 3: Evidence and Verification

**Files:**
- Create: `docs/superpowers/verification/2026-06-06-p0.71-mobile-final-resources-preflight-regression.md`
- Create: `docs/superpowers/verification/p0.71-mobile-final-resources-preflight.html`
- Create: `docs/superpowers/verification/assets/p0.71-mobile-final-resources-preflight-390x844.png`

- [x] **Step 1: Add static visual evidence**

Create a 390x844 evidence page showing the new Device Preflight row.

- [x] **Step 2: Run full verification**

Run:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: all pass, with the existing Starlette/TestClient deprecation warning allowed.
