# P0.583 Final Refresh Live Action Pruning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Make `final-local-report-refresh.operator_actions` use the guarded live provider consent command as the only top-level live provider entry point when it is present.

**Architecture:** Reuse the existing final-local report operator action normalization pipeline. Add a narrow preference helper after provider chain normalization and before deploy/device prioritization.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_local_report_refresh.py`

- [x] Add a regression test with a consent-required next action plus provider fallback actions.
- [x] Assert the final operator action list keeps only the guarded live consent command for provider execution.
- [x] Confirm the new test fails on the old implementation.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_local_report_refresh.py`

- [x] Add a helper that detects the guarded live provider consent action.
- [x] Filter provider handoff and bare live-provider fallback actions only when that consent action is present.
- [x] Preserve unrelated operator actions.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.583 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused final-local report refresh tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect `final-local-report-refresh.json.operator_actions`.

Safe refresh note: after temporarily running `backend-device-demo`, `final-local-report-refresh.json.operator_actions` starts with `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make final-acceptance-configured; rerun make live-provider-evidence` and no longer includes provider-handoff or bare live-provider-evidence fallback actions. `final-showcase-readiness.json` remains `partial` with live Meshy/OpenAI evidence as the first blocker.
