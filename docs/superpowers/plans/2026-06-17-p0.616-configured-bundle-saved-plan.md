# P0.616 Configured Bundle Saved Plan

## Steps

1. Inspect current readiness reports and confirm saved plan versus bundle disagreement.
2. Add failing coverage for saved configured-plan reuse.
3. Load the saved configured plan only when no explicit settings are passed and live calls are not allowed.
4. Run focused, adjacent, lint, and backend regression gates.
5. Refresh safe local evidence, merge to `main`, and push.

## Verification

- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_configured_live_evidence_bundle.py::test_configured_live_evidence_bundle_uses_saved_configured_plan`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_configured_live_evidence_bundle.py`
- `make backend-lint`
- `make backend-test`
