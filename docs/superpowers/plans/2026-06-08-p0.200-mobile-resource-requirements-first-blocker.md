# P0.200 Mobile Resource Requirements First Blocker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Decode and display `final_resource_requirements.first_blocker` on iPhone final launch summaries.

**Architecture:** Mirror backend snake_case with a small Codable struct. Keep rendering inside `FinalLaunchMobileSummaryBuilder.resourceRequirementRows` and preserve existing requirement-row fallback.

**Tech Stack:** SwiftPM executable contract tests and ProjectChecks.

---

### Task 1: Swift Contract

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [x] **Step 1: Add red contract expectations**

Extend `testDecodesFinalResourceRequirementsFromFinalLaunchPayload` to assert `firstBlocker?.id == "MESHY_API_KEY"`.

Add/extend a summary test to assert `resourceRequirementRows` contains the first blocker command and detail.

- [x] **Step 2: Run red Swift contract**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: fail because the model does not decode `first_blocker`.

- [x] **Step 3: Implement model and rows**

Add `FinalResourceRequirementsFirstBlocker`, optional field, initializer parameter, and first-blocker row helper.

- [x] **Step 4: Run green Swift contract**

Run the same Swift contract executable.

### Task 2: Source Gate

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [x] **Step 1: Require model/summary/test source tokens**

Require:
- `FinalResourceRequirementsFirstBlocker`
- `firstBlocker: FinalResourceRequirementsFirstBlocker?`
- `requirements.firstBlocker`
- relevant contract test name/token

- [x] **Step 2: Run ProjectChecks**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Expected: pass.

### Task 3: Verification

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py tests/test_ios_showcase_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Expected: all pass with only the existing Starlette/httpx warning from backend tests.
