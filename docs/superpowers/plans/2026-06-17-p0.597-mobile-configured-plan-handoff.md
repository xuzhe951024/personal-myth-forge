# P0.597 Mobile Configured Plan Handoff Plan

## Goal

Surface configured evidence plan live-provider commands in the iPhone Final Showcase
Provider Handoff stage.

## Steps

1. Read current git state, product docs, roadmap, and saved readiness reports.
2. Add a RED Swift contract test for Provider Handoff detail using configured plan
   guarded live actions.
3. Update `FinalShowcaseSummary` provider handoff row selection to include configured
   evidence plan rows and prefer concrete guarded make commands.
4. Run mobile contract tests plus focused backend/source acceptance checks as needed.
5. Refresh safe local readiness reports, then commit, merge, and push.

## Safety

This phase only changes mobile summary presentation and tests. It must not run live
provider commands, write secrets, submit print orders, or touch Xcode signing/global
state.
