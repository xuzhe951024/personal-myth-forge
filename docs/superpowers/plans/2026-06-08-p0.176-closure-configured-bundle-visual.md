# P0.176 Closure Configured Bundle Visual Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the closure packet visual regression artifact prove the configured evidence bundle row is visible.

**Architecture:** Tighten `p0.157_final_launch_closure_packet` required text in `visual_regression.py`, then update the existing checked-in HTML and notes to match. The PNG dimensions remain unchanged and no runtime UI code changes.

**Tech Stack:** Python visual regression checker, pytest, checked-in HTML/PNG/Markdown visual artifacts.

---

## File Structure

- Modify `services/backend/tests/test_visual_regression.py` to add red assertions for the configured bundle closure text.
- Modify `services/backend/src/myth_forge_api/visual_regression.py` to require configured bundle text in `p0.157_final_launch_closure_packet`.
- Modify `docs/superpowers/verification/p0.157-final-launch-closure-packet.html` to show the configured evidence bundle row and section count.
- Modify `docs/superpowers/verification/2026-06-08-p0.157-final-launch-closure-packet-regression.md` to document the new visual proof.
- No Swift runtime, backend provider, resource writer, live API, Xcode, or global configuration changes.

## Task 1: Red Visual Required Text

**Files:**
- Modify: `services/backend/tests/test_visual_regression.py`

- [ ] **Step 1: Add required text assertions**

In `test_default_visual_artifacts_cover_full_showcase_flow`, after:

```python
assert "provide MESHY_API_KEY" in required_text
```

add:

```python
assert "configured_evidence_bundle" in required_text
assert "configured_live_evidence_bundle" in required_text
assert "make configured-live-evidence-bundle" in required_text
```

- [ ] **Step 2: Run red test**

Run:

```bash
cd services/backend
uv run pytest tests/test_visual_regression.py -q
```

Expected: FAIL because `DEFAULT_VISUAL_ARTIFACTS` does not yet require those strings.

## Task 2: Update Visual Artifact Contract

**Files:**
- Modify: `services/backend/src/myth_forge_api/visual_regression.py`
- Modify: `docs/superpowers/verification/p0.157-final-launch-closure-packet.html`
- Modify: `docs/superpowers/verification/2026-06-08-p0.157-final-launch-closure-packet-regression.md`

- [ ] **Step 1: Tighten required text**

In `p0.157_final_launch_closure_packet.required_text`, add these strings after `provide MESHY_API_KEY`:

```python
"configured_evidence_bundle",
"configured_live_evidence_bundle",
"make configured-live-evidence-bundle",
```

- [ ] **Step 2: Update the checked-in HTML**

In `docs/superpowers/verification/p0.157-final-launch-closure-packet.html`, change:

```html
<p class="line strong">Final closure blocked: sections 5, ready 0, blocked 5, required actions 12.</p>
```

to:

```html
<p class="line strong">Final closure blocked: sections 6, ready 0, blocked 5, required actions 13.</p>
```

After the `resource_inputs` line, add:

```html
<p class="line strong">Configured evidence: configured_evidence_bundle | configured_live_evidence_bundle | make configured-live-evidence-bundle</p>
```

- [ ] **Step 3: Update the notes**

In `docs/superpowers/verification/2026-06-08-p0.157-final-launch-closure-packet-regression.md`, add these bullets under "The visual proof shows":

```markdown
- `configured_evidence_bundle`
- `configured_live_evidence_bundle`
- `make configured-live-evidence-bundle`
```

- [ ] **Step 4: Run green visual regression tests**

Run:

```bash
cd services/backend
uv run pytest tests/test_visual_regression.py -q
```

Expected: PASS.

## Task 3: Verification And Commit

**Files:**
- Verify all modified files.

- [ ] **Step 1: Run focused checks**

Run:

```bash
cd services/backend
uv run pytest tests/test_visual_regression.py tests/test_ios_showcase_acceptance.py -q
```

Expected: all focused tests pass with the known Starlette/httpx warning.

- [ ] **Step 2: Run backend lint**

Run:

```bash
make backend-lint
```

Expected: `All checks passed!`

- [ ] **Step 3: Commit implementation**

Run:

```bash
git add services/backend/tests/test_visual_regression.py \
  services/backend/src/myth_forge_api/visual_regression.py \
  docs/superpowers/verification/p0.157-final-launch-closure-packet.html \
  docs/superpowers/verification/2026-06-08-p0.157-final-launch-closure-packet-regression.md
git commit -m "test: cover closure configured bundle visual"
```

## Task 4: Merge Back To Main

**Files:**
- No direct file edits.

- [ ] **Step 1: Verify branch status**

Run:

```bash
git status --short --branch
```

Expected: clean branch `codex/p0.176-closure-configured-bundle-visual`.

- [ ] **Step 2: Merge into main**

Run from `/Users/zhexu/Codex/personal-myth-forge`:

```bash
git merge --no-ff codex/p0.176-closure-configured-bundle-visual
```

Expected: merge commit created.

- [ ] **Step 3: Post-merge smoke**

Run:

```bash
cd services/backend
uv run pytest tests/test_visual_regression.py tests/test_ios_showcase_acceptance.py -q
make backend-lint
```

Expected: focused tests and lint pass on `main`.

## Self-Review

- Spec coverage: all acceptance bullets map to required text assertions, visual artifact changes, notes changes, or verification.
- Placeholder scan: no TBD/TODO/open-ended implementation steps.
- Type consistency: artifact id remains `p0.157_final_launch_closure_packet`; required strings match P0.174 closure packet section/action/command ids.
- Scope check: no runtime UI, provider, live API, Xcode, signing, or resource writer behavior changes.
