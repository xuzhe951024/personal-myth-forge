# P0.441 Showcase Provider Handoff Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Keep `make provider-handoff` visible in the iPhone Final Launch showcase readiness action list.

**Architecture:** Reuse the existing `FinalLaunchMobileSummaryBuilder.showcaseReadinessActionRows(_:)` priority filter. Add provider handoff fragments to the filter, raise the selected action cap from 7 to 8 so print handoff remains visible, and cover the behavior in the existing Swift contract suite.

**Tech Stack:** Swift Package Manager, `PersonalMythForgeMobileCore`, `PersonalMythForgeMobileCoreContractTests`.

---

### Task 1: Contract Coverage

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [ ] **Step 1: Add the failing provider handoff assertion**

In `testFinalLaunchMobileSummaryShowsPriorityFinalShowcaseActions()`, add
`"make provider-handoff"` to the `finalShowcaseReadinessActions` fixture after
the live-provider action. Add this assertion:

```swift
try expectContains(text, "make provider-handoff")
```

Because the visible row count increases by one after implementation, update the
expected count from `15` to `16`.

- [ ] **Step 2: Run the focused Swift contract binary**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: FAIL with the provider handoff assertion missing from the summary.

### Task 2: Priority Filter

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [ ] **Step 1: Add provider handoff fragments**

In `showcaseReadinessActionRows(_:)`, add these values to
`priorityFragments`:

```swift
"provider-handoff",
"provider_handoff",
```

Change the returned cap from:

```swift
return deduped(selected).prefix(7).map(sanitize)
```

to:

```swift
return deduped(selected).prefix(8).map(sanitize)
```

- [ ] **Step 2: Run the focused Swift contract binary**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: PASS.

### Task 3: Roadmap And Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Record the roadmap line**

Append:

```markdown
- P0.441 iPhone Final Launch prioritizes provider handoff actions
```

- [ ] **Step 2: Run verification**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios
make backend-lint
git diff --check
```

Expected: all commands exit `0`.

- [ ] **Step 3: Commit**

Run:

```bash
git add apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift \
  apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift \
  docs/iteration-roadmap.md \
  docs/superpowers/specs/2026-06-13-p0.441-showcase-provider-handoff-action-design.md \
  docs/superpowers/plans/2026-06-13-p0.441-showcase-provider-handoff-action.md
git commit -m "feat: prioritize showcase provider handoff"
```
