# P0.622 Configured Bundle Demo Fallback Pruning Plan

## Goal

Keep `configured-live-evidence-bundle` operator actions focused on the configured live evidence consent sequence, without retaining configured demo-launch fallback commands.

## Steps

1. Add a focused failing test that injects `make final-demo-launch-configured; rerun make final-configured-evidence-plan` alongside configured consent actions.
2. Update fallback action detection to compare the command root before validation suffixes.
3. Run the focused test to confirm GREEN.
4. Run `test_configured_live_evidence_bundle.py`, lint, and backend tests.
5. Refresh safe local evidence without live provider calls and inspect the configured bundle operator actions.
6. Merge to `main` and push.

## Safety

- Do not execute any `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 ...` command.
- Do not call Meshy, OpenAI, Treatstock, or any paid external provider.
- Do not modify Keychain, Xcode signing, or global developer state.
