# P0.416 Configured Evidence Device Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a top-level `device_action_bundle` to `final_configured_evidence_plan_report` by safely copying the nested configured preflight device bundle.

**Architecture:** `final_configured_evidence_plan.py` already builds `configured_preflight`; add a local copier that maps `configured_preflight.device_action_bundle` into the evidence-plan report with a new report-specific id/label/source. Tests assert the report contract without changing step ordering or live-provider consent behavior.

**Tech Stack:** Python 3.10, FastAPI backend package, pytest, existing Make/CLI report targets.

---

### Task 1: Add Failing Contract Test

**Files:**
- Modify: `services/backend/tests/test_final_configured_evidence_plan.py`

- [ ] **Step 1: Add the failing test**

Add this test after `test_configured_evidence_plan_blocks_missing_resources_without_running_commands`:

```python
def test_configured_evidence_plan_exposes_device_action_bundle(
    tmp_path: Path,
) -> None:
    result = build_final_configured_evidence_plan_report(repo_root=tmp_path)
    report_text = json.dumps(result.report)

    bundle = result.report["device_action_bundle"]

    assert bundle["id"] == "final_configured_evidence_plan_device_actions"
    assert bundle["label"] == "Final Configured Evidence Plan Device Actions"
    assert bundle["source_report"] == "final_configured_preflight"
    assert bundle["status"] == "blocked"
    assert bundle["summary"]["actions"] == 7
    assert bundle["summary"]["provider_calls"] == 0
    assert bundle["summary"]["global_actions"] == 0
    assert bundle["summary"]["xcode_or_signing"] == 1
    assert bundle["first_action"]["id"] == "write_development_team"
    assert bundle["first_action"]["command"] == (
        "DEVELOPMENT_TEAM=YOUR_TEAM_ID make mobile-write-deploy-config-auto"
    )
    assert bundle["first_action"]["validation_command"] == (
        "make mobile-deploy-preflight"
    )
    assert bundle["safety"]["commands_run"] is False
    assert bundle["safety"]["provider_calls"] is False
    assert bundle["safety"]["xcode_or_signing"] is False
    assert str(tmp_path) not in report_text
    assert "sk-" not in report_text
```

- [ ] **Step 2: Run RED**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_configured_evidence_plan.py::test_configured_evidence_plan_exposes_device_action_bundle -q
```

Expected: FAIL with `KeyError: 'device_action_bundle'`.

### Task 2: Implement Device Bundle Copy

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_configured_evidence_plan.py`

- [ ] **Step 1: Add the report field**

In `build_final_configured_evidence_plan_report`, add:

```python
"device_action_bundle": _device_action_bundle(configured_preflight),
```

immediately after `"next_action": _next_action(first_blocker),`.

- [ ] **Step 2: Add helper functions**

Add `_device_action_bundle`, `_missing_device_action_bundle`, `_device_action`, `_device_next_action`, `_device_first_action`, `_device_action_summary`, `_device_action_safety`, and `_device_action_status` near `_safety()`.

Use:

```python
"id": "final_configured_evidence_plan_device_actions"
"label": "Final Configured Evidence Plan Device Actions"
"source_report": "final_configured_preflight"
```

The helper should copy optional action fields `blocks`, `evidence_status`, `evidence_source`, `evidence_detail`, `validation_command`, `operator_actions`, `required`, and `requires_consent`, plus sanitized nested `next_action` fields.

- [ ] **Step 3: Run GREEN for the new test**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_configured_evidence_plan.py::test_configured_evidence_plan_exposes_device_action_bundle -q
```

Expected: PASS.

### Task 3: Verify Focused File And Saved Report

**Files:**
- Modify: `docs/iteration-roadmap.md`
- Generated ignored output: `services/backend/.local/final-configured-evidence-plan.json`

- [ ] **Step 1: Add roadmap entry**

Add this line after P0.415:

```markdown
- P0.416 final configured evidence plan exposes device action bundle
```

- [ ] **Step 2: Run focused tests**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_configured_evidence_plan.py -q
```

Expected: all focused tests pass.

- [ ] **Step 3: Refresh configured evidence plan JSON**

Run:

```bash
make final-configured-evidence-plan
```

Expected: exit `2` while resources are missing; `services/backend/.local/final-configured-evidence-plan.json` is still written.

- [ ] **Step 4: Assert saved JSON shape**

Run:

```bash
python3 - <<'PY'
import json
from pathlib import Path
p=Path("services/backend/.local/final-configured-evidence-plan.json")
d=json.loads(p.read_text())
b=d["device_action_bundle"]
fa=b["first_action"]
assert b["id"] == "final_configured_evidence_plan_device_actions"
assert b["source_report"] == "final_configured_preflight"
assert b["status"] == "blocked"
assert fa["command"] == "DEVELOPMENT_TEAM=YOUR_TEAM_ID make mobile-write-deploy-config-auto"
assert fa["validation_command"] == "make mobile-deploy-preflight"
print(b["id"], b["source_report"], b["status"], fa["id"])
PY
```

Expected output includes `final_configured_evidence_plan_device_actions final_configured_preflight blocked write_development_team`.

### Task 4: Final Verification

**Files:**
- Read-only verification across backend.

- [ ] **Step 1: Run lint**

Run:

```bash
make backend-lint
```

Expected: `All checks passed!`.

- [ ] **Step 2: Run backend tests**

Run:

```bash
make backend-test
```

Expected: all backend tests pass; the known FastAPI/Starlette `TestClient` deprecation warning may remain.

- [ ] **Step 3: Merge and push**

Run:

```bash
git status --short
git add docs/iteration-roadmap.md docs/superpowers/specs/2026-06-13-p0.416-configured-evidence-device-bundle-design.md docs/superpowers/plans/2026-06-13-p0.416-configured-evidence-device-bundle.md services/backend/src/myth_forge_api/final_configured_evidence_plan.py services/backend/tests/test_final_configured_evidence_plan.py
git commit -m "feat: add configured evidence device actions"
git switch main
git merge --ff-only codex/p0.416-configured-evidence-device-bundle
git push origin main
```

Expected: main advances to the new commit on `origin/main`.
