# P0.377 iOS Completion Consent Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make iOS decode and display final showcase completion-level live provider consent for partial 3D/NPC readiness rows.

**Architecture:** Add an optional field to the existing Swift Codable model. Render the field through the existing Final Launch summary row builder so the current iPhone summary screen gains the consent hint without a new view.

**Tech Stack:** Swift Package Manager, Swift Codable with `convertFromSnakeCase`, existing mobile core contract test executable.

---

### Task 1: Add Red Swift Contract Coverage

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [ ] **Step 1: Add payload field and decode assertion**

In `finalDemoLaunchPayload`, add the backend field to a non-ready `game_asset_3d_generation` capability:

```swift
"completion_requires_live_provider_consent": true
```

In `testDecodesFinalShowcaseReadinessFromFinalLaunchPayload`, assert:

```swift
let threeDCapability = try require(
    readiness.capabilities.first(where: { $0.id == "game_asset_3d_generation" }),
    "missing 3D final showcase capability"
)
try expectEqual(threeDCapability.completionRequiresLiveProviderConsent, true)
```

- [ ] **Step 2: Add summary assertion**

In `testFinalLaunchMobileSummaryShowsFinalShowcaseReadiness`, assert one row contains:

```swift
"completion live provider consent required"
```

- [ ] **Step 3: Run focused contract test to verify RED**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: compile failure because `completionRequiresLiveProviderConsent` does not exist yet.

### Task 2: Decode And Render Completion Consent

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [ ] **Step 1: Extend the model**

Add to `FinalShowcaseReadinessCapability`:

```swift
public var completionRequiresLiveProviderConsent: Bool?
```

Add an initializer argument with default:

```swift
completionRequiresLiveProviderConsent: Bool? = nil
```

Assign it in the initializer:

```swift
self.completionRequiresLiveProviderConsent = completionRequiresLiveProviderConsent
```

- [ ] **Step 2: Extend the summary row**

In `showcaseReadinessCapabilityRow`, after appending the command, add:

```swift
if capability.completionRequiresLiveProviderConsent == true {
    parts.append("| completion live provider consent required")
}
```

- [ ] **Step 3: Run focused contract test to verify GREEN**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: contract tests pass.

### Task 3: Verify Adjacent Mobile Checks

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Run Swift project checks**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Expected: pass.

- [ ] **Step 2: Run Swift compile check**

Run:

```bash
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Expected: pass.

- [ ] **Step 3: Update roadmap**

Add:

```markdown
- P0.377 iOS final launch summary decodes and displays completion-level live provider consent
```

- [ ] **Step 4: Run whitespace check**

Run:

```bash
git diff --check
```

Expected: no output and exit 0.

- [ ] **Step 5: Commit**

Run:

```bash
git add apps/mobile/ios/Sources/PersonalMythForgeMobileCore/PMFModels.swift apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift docs/iteration-roadmap.md docs/superpowers/specs/2026-06-12-p0.377-ios-completion-consent-design.md docs/superpowers/plans/2026-06-12-p0.377-ios-completion-consent.md
git commit -m "fix: show iOS completion consent"
```
