# P0.620 Local Refresh Granular Configured Consent

## Steps

1. Confirm current `final-local-report-refresh` uses the broad configured final acceptance live action while other reports use configured 3D evaluation.
2. Add/update failing coverage for granular configured evidence consent actions.
3. Update final local refresh hint projection to guard the specific configured evidence command instead of replacing it.
4. Run focused, adjacent, lint, and backend regression gates.
5. Refresh safe local evidence, merge to `main`, and push.

## Verification

- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_local_report_refresh.py::test_final_local_report_refresh_guards_configured_evidence_consent_action`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_local_report_refresh.py`
- `make backend-lint`
- `make backend-test`
