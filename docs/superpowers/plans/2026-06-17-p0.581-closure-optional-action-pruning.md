# P0.581 Closure Optional Action Pruning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Keep optional resource rows out of the final launch closure packet's top-level operator action queue so the remaining live provider consent action is not buried behind non-required work.

**Architecture:** Preserve optional rows inside their sections for auditability. Prune them only when collecting `operator_actions`.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_launch_closure_packet.py`

- [x] Add a focused test with optional resource and print actions plus a blocked live consent action.
- [x] Assert the live consent action is first.
- [x] Assert optional resource and print actions are not promoted to top-level `operator_actions`.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_launch_closure_packet.py`

- [x] Update `_operator_actions()` to skip `optional` actions.
- [x] Keep existing behavior for blocked, missing, partial, live, and manual actions.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.581 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused closure packet tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect final closure/operator actions.

Safe refresh note: after refreshing with `backend-device-demo` temporarily running, `final-launch-closure-packet.json.operator_actions` contains only the guarded live consent action and configured evidence bundle refresh; optional resource and print actions are absent from the top-level queue.
