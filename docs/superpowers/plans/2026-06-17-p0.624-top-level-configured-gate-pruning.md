# P0.624 Top-Level Configured Gate Pruning Plan

## Goal

Keep top-level final handoff reports focused on the concrete configured live provider consent command once that command is available.

## Steps

1. Add focused failing tests for closure packet and external action ledger operator actions.
2. Add pruning helpers that remove the configured bundle gate fallback only when a granular configured live consent action is present.
3. Run focused tests to confirm GREEN.
4. Run adjacent closure/external suites, lint, and full backend tests.
5. Refresh safe local evidence and inspect top-level operator actions.
6. Merge to `main` and push.

## Safety

- Do not execute `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 ...`.
- Do not call Meshy, OpenAI, Treatstock, or any paid provider.
- Do not change Xcode signing, Keychain, global env, or real order state.
