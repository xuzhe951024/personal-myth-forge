# P0.204 Device Preflight First Blocker Redaction Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Add regression coverage and source gates for final resource fill guide first blocker redaction.

---

### Task 1: Source Gate RED

**Files:**
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`

- [x] **Step 1: Add red source-gate assertion**

Require `contract_final_resource_fill_guide_first_blocker_redaction` in the mobile final launch readiness requirement IDs.

- [x] **Step 2: Run red backend test**

Run focused backend readiness acceptance test. Expected: fail because the requirement is not implemented yet.

### Task 2: Contract And Gates

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`

- [x] **Step 1: Add first blocker redaction contract test**

Inject unsafe first blocker command/detail into the fill guide report and assert encoded preflight output is redacted.

- [x] **Step 2: Add source requirements**

Require the new contract test from backend readiness acceptance and ProjectChecks.

### Task 3: Verification

- [x] **Step 1: Run worktree verification**

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Record observed results after verification.

Observed: all commands passed. Backend pytest and local rehearsal retained the existing Starlette/httpx deprecation warning; configured acceptance was dry-run only.
