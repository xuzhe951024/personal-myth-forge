# Provider Chain Normalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development and superpowers:verification-before-completion. This is a narrow report-normalization phase; do not invoke live providers or global/Xcode actions.

**Goal:** Make final external action ledger and final launch closure packet prefer the complete provider-key handoff chain before live provider evidence.

**Architecture:** Reuse the proven final showcase readiness behavior. Add a local provider-chain preference helper to the two final operator action aggregators, after existing dedupe/apply-preview normalization and before device action promotion.

**Tech Stack:** Python backend report builders, pytest, Make safe wrappers, ignored `.local` evidence files.

---

### Task 1: Final External Ledger Provider Chain

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_external_action_ledger.py`
- Test: `services/backend/tests/test_final_external_action_ledger.py`

- [x] **Step 1: Write the failing test**

Update the provider child action test so it expects:

```python
"make final-resource-apply-preview; rerun make provider-handoff; rerun make live-provider-evidence"
```

and rejects:

```python
"make final-resource-apply-preview; rerun make live-provider-evidence"
"make provider-handoff; rerun make live-provider-evidence"
```

- [x] **Step 2: Run RED**

Run the focused test and confirm it fails before implementation.

- [x] **Step 3: Implement minimal helper**

Add provider-chain constants/helper to remove weak provider actions when the full chain is present. Apply it in `_operator_actions()`.

- [x] **Step 4: Run GREEN**

Run the focused external ledger test again and confirm it passes.

### Task 2: Final Launch Closure Packet Provider Chain

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_launch_closure_packet.py`
- Test: `services/backend/tests/test_final_launch_closure_packet.py`

- [x] **Step 1: Write the failing test**

Update the ledger child operator action test with the same complete-chain expectations.

- [x] **Step 2: Run RED**

Run the focused closure packet test and confirm it fails before implementation.

- [x] **Step 3: Implement minimal helper**

Apply the same provider-chain preference in closure packet `_operator_actions()` while preserving print and iOS deploy writer normalization.

- [x] **Step 4: Run GREEN**

Run the focused closure packet test again and confirm it passes.

### Task 3: Verification and Handoff

**Files:**
- Modify: `docs/iteration-roadmap.md`
- Modify: `docs/superpowers/plans/2026-06-14-p0.484-provider-chain-normalization.md`

- [x] **Step 1: Update roadmap**

Add:

```markdown
- P0.484 Final external and closure action lists prefer complete provider handoff chains
```

- [x] **Step 2: Run focused verification**

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_final_external_action_ledger.py tests/test_final_launch_closure_packet.py -q
```

- [x] **Step 3: Refresh safe local evidence**

Run:

```bash
make final-external-action-ledger
make final-launch-closure-packet
make final-showcase-readiness
```

Expected: evidence remains blocked on real external resources, but provider operator actions consistently use the full provider handoff chain.

- [x] **Step 4: Commit, merge, push**

Fast-forward merge into `main`, push `origin/main`, and leave the goal active unless every final objective requirement has current evidence.
