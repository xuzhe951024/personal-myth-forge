# P0.626 Configured Preflight Granular Live Action Plan

## Steps

1. Add a focused failing test for configured resources plus missing live evidence.
2. Update `final_configured_preflight` operator action selection to pull granular
   live consent actions from the configured final launch/showcase reports.
3. Prune broad live-provider and configured final-acceptance actions when granular
   actions are available.
4. Run the focused test, the full configured preflight test file, backend lint, and
   backend tests.
5. Refresh safe local reports and merge/push the branch.
