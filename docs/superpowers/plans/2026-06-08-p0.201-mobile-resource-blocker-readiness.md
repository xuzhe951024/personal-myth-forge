# P0.201 Mobile Resource Blocker Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Make mobile final launch readiness acceptance protect the iOS resource requirements first blocker contract.

**Architecture:** Add source requirements to `mobile_final_launch_readiness_acceptance.py` and matching minimal fixture tokens in the acceptance tests. No runtime backend or Swift behavior changes are required.

**Tech Stack:** Python pytest source acceptance plus existing Swift ProjectChecks.

---

### Task 1: Acceptance Contract

**Files:**
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`

- [x] **Step 1: Add red requirement assertions**

Add assertions for these IDs in `test_mobile_final_launch_readiness_acceptance_checks_endpoint_source_and_safety`:

```python
assert "model_final_resource_requirements_first_blocker" in requirement_ids
assert "model_final_resource_requirements_first_blocker_field" in requirement_ids
assert "mobile_summary_final_resource_requirements_source" in requirement_ids
assert "mobile_summary_final_resource_requirements_first_blocker_row" in requirement_ids
assert "contract_final_resource_requirements_first_blocker_fixture" in requirement_ids
```

- [x] **Step 2: Run red pytest**

Run:

```bash
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py::test_mobile_final_launch_readiness_acceptance_checks_endpoint_source_and_safety -q
```

Observed: failed first on `model_final_resource_requirements_first_blocker`, then on `mobile_summary_final_resource_requirements_source` before the corresponding source requirements were added.

- [x] **Step 3: Add source requirements and fixture tokens**

Add requirements for:
- `FinalResourceRequirementsFirstBlocker`
- `firstBlocker: FinalResourceRequirementsFirstBlocker?`
- `report.finalResourceRequirements`
- `resourceRequirementFirstBlockerRow`
- `provide MESHY_API_KEY in final-resources.env`

- [x] **Step 4: Run green pytest**

Run the focused test and full mobile readiness acceptance test file.

### Task 2: Verification

- [x] **Step 1: Run worktree verification**

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py tests/test_ios_showcase_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Observed: all passed, with only the existing Starlette/httpx warning from backend commands. `make final-rehearsal-local` still refreshes local reports and leaves the first blocker pointing to the Meshy key handoff.
