# P0.585 Final Refresh Showcase Settings Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Preserve configured provider/key handoff evidence when `final-local-report-refresh` rewrites `final-showcase-readiness.json`.

**Architecture:** Change only the final showcase readiness step inside `final_local_report_refresh._default_steps()`. Local evaluation steps continue to use deterministic local providers.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_local_report_refresh.py`

- [x] Add a focused unit test for the final showcase refresh step.
- [x] Monkeypatch `load_settings()` with configured provider settings.
- [x] Confirm the old implementation passes `LOCAL_SETTINGS` and fails the test.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_local_report_refresh.py`

- [x] Use `load_settings()` for `build_final_showcase_readiness_report`.
- [x] Leave local 3D/NPC evaluation steps on `LOCAL_SETTINGS`.
- [x] Do not add any live provider calls.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.585 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused final-local report refresh tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and confirm final showcase keeps configured provider/key handoff state.

Safe refresh note: `final-local-report-refresh` now rewrites `final-showcase-readiness.json` with configured backend settings preserved. The saved final showcase report remains `partial`, with `provider_key_handoff` classified as `live_provider_evidence_unproven` and routed through `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make final-acceptance-configured; rerun make live-provider-evidence`.
