# P0.611 Closure Live Confirmation Plan

## Steps

1. Inspect current final readiness and closure packet reports after local evidence refresh.
2. Add a failing focused test for configured live evidence closure metadata.
3. Implement the smallest shared normalization in `_closure_action`.
4. Run focused and adjacent closure packet tests.
5. Run source gates, commit, merge to `main`, and push.

## Verification

- `cd services/backend && uv run pytest tests/test_final_launch_closure_packet.py::test_final_launch_closure_packet_routes_configured_evidence_through_live_consent`
- `cd services/backend && uv run pytest tests/test_final_launch_closure_packet.py`
- Adjacent final handoff tests after implementation.

