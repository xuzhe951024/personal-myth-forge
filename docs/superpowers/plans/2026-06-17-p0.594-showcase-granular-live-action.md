# P0.594 Showcase Granular Live Action Plan

## Goal

Make the final showcase readiness handoff point at the first configured evidence
step that actually needs live-provider consent.

## Steps

1. Read current git state, product docs, roadmap, and saved readiness reports.
2. Add a RED test for final showcase rows preferring the configured evidence plan
   next action.
3. Load and summarize `final-configured-evidence-plan.json` inside the final
   showcase readiness report.
4. Route live-provider partial rows through that configured evidence plan action
   when present.
5. Run focused tests, lint, refresh safe local reports, then commit and push.

## Safety

This phase only changes saved report command routing. It must not run commands with
`PMF_ALLOW_LIVE_PROVIDER_CALLS=1`, contact Meshy/OpenAI/Treatstock, write secrets,
or perform Xcode signing/global Mac changes.
