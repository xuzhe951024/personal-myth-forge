# P0.631 Handoff Index Action Command Plan

## Steps

1. Add a RED test proving local rehearsal fallback action should be a command, not
   lane detail text.
2. Change the local rehearsal operator action fallback to `make final-rehearsal-local`
   while leaving first blocker detail unchanged.
3. Run focused tests, related handoff tests, lint, full backend tests, and safe report
   refresh commands.
4. Merge to `main` and push with P0.630.
