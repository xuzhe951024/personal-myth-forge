# P0.256 Final Showcase Blocker Concrete Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Align final showcase blocker detail with the concrete mobile preflight child next action.

**Architecture:** Reuse the existing `device_action_bundle` and `next_action` metadata already built by final showcase readiness. Keep phase/blocker identity unchanged and only change the action string selected for the detail hint.

**Tech Stack:** Python, FastAPI backend support modules, pytest.

---

### Task 1: Final Showcase Blocker Detail

**Files:**
- Modify: `services/backend/tests/test_final_showcase_readiness.py`
- Modify: `services/backend/src/myth_forge_api/final_showcase_readiness.py`
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Write the failing test**

Add assertions to `test_final_showcase_readiness_next_action_uses_preflight_child_action`:

```python
blocker = result.report["first_blocker"]
assert blocker["command"] == "make ios-device-launch-rehearsal"
assert (
    "Next device action: provide DEVELOPMENT_TEAM in Deployment.local.xcconfig"
    in blocker["detail"]
)
assert "Next device action: make mobile-deploy-preflight" not in blocker["detail"]
assert action["detail"] == blocker["detail"]
```

- [ ] **Step 2: Run test to verify RED**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_showcase_readiness.py::test_final_showcase_readiness_next_action_uses_preflight_child_action -q
```

Expected: fails because blocker detail still uses `make mobile-deploy-preflight`.

- [ ] **Step 3: Implement the selector**

Update `_first_device_action_hint()` to use the child next action command when present:

```python
child_next_action = _device_action_child_next_action(action)
command = (
    str(child_next_action.get("command", "")).strip()
    or str(action.get("command", "")).strip()
)
```

Keep `detail` sourced from `evidence_detail`.

- [ ] **Step 4: Run focused GREEN**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_showcase_readiness.py -q
```

Expected: all tests pass.

- [ ] **Step 5: Verify wider backend gates**

Run:

```bash
make backend-lint
make backend-test
git diff --check
```

Expected: lint passes, backend tests pass, no whitespace errors.

- [ ] **Step 6: Commit**

```bash
git add services/backend/tests/test_final_showcase_readiness.py \
  services/backend/src/myth_forge_api/final_showcase_readiness.py \
  docs/iteration-roadmap.md \
  docs/superpowers/specs/2026-06-11-p0.256-showcase-blocker-concrete-action-design.md \
  docs/superpowers/plans/2026-06-11-p0.256-showcase-blocker-concrete-action.md
git commit -m "Use concrete final showcase blocker action"
```
