# P0.579 Final Refresh Consent Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Ensure `final-local-report-refresh` never promotes a bare live-provider configured evaluation command when a configured evidence blocker requires live provider consent.

**Architecture:** Add a narrow hint normalization path in `final_local_report_refresh.py` for `final_configured_evidence_plan_report` actions that require live provider consent or carry cost risk. The normalized command uses the same guarded live-provider consent action as final showcase readiness while preserving detail and validation metadata.

**Tech Stack:** Python 3.10, pytest, backend report builders, Make safe report wrappers.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_local_report_refresh.py`

- [x] Add a test with `extra_steps={"final_configured_evidence_plan": consent_required_plan}`.
- [x] Assert the step command is the guarded live-provider handoff.
- [x] Assert the step `next_action` and executable `operator_actions` do not expose `make backend-evaluate-3d-configured`.

### Task 2: Consent Hint Normalization

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_local_report_refresh.py`

- [x] Add a helper that detects `final_configured_evidence_plan_report` actions where `requires_live_provider_consent` or `cost_risk` is true.
- [x] In `_hint_from_report_action()`, replace the command with the guarded live provider consent action for those actions.
- [x] Keep validation and detail fields intact.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] Add the P0.579 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused tests:

```bash
cd services/backend && uv run pytest tests/test_final_local_report_refresh.py -q
```

- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect that final-local refresh first action is guarded.
