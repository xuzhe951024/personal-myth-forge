# P0.591 Handoff Index Live Consent Action Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Follow TDD and update this checklist as work completes.

**Goal:** Align `final-handoff-index` live-provider operator actions with the guarded live acceptance handoff used by the final reports.

**Architecture:** Keep the existing configured acceptance make target. Change only the final handoff index's operator action text and priority classification.

## Tasks

- [x] Read current git state, design docs, roadmap, and saved `.local` readiness reports.
- [x] Identify final-handoff-index as the remaining old live consent text source.
- [x] Add RED coverage expecting the guarded live acceptance action and rejecting the old review-only text.
- [x] Confirm RED with the focused final handoff index test.
- [x] Add a final handoff index guarded live action constant.
- [x] Normalize imported old cost-review text to the guarded live action.
- [x] Keep guarded final acceptance out of generic provider handoff priority ordering.
- [x] Confirm GREEN with focused and related tests.
- [x] Run backend lint and diff checks.
- [x] Refresh local handoff/readiness reports and confirm saved operator action text.
- [x] Merge to `main`, commit, and push.

## Verification Commands

```bash
cd services/backend
uv run pytest tests/test_final_handoff_index.py::test_final_handoff_index_blocks_missing_reports_without_leaks -q
uv run pytest tests/test_final_handoff_index.py tests/test_configured_acceptance_command_handoff.py -q
```

Additional final checks:

```bash
make backend-lint
git diff --check
```
