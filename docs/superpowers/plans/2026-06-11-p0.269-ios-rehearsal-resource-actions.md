# P0.269 iOS Rehearsal Resource Actions Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make iOS device launch rehearsal reports preserve validation commands for resource handoff actions.

**Architecture:** Add a local normalization helper in `ios_device_launch_rehearsal.py` and mirror it in `ios_device_launch_rehearsal_readiness.py` for saved reports. The helper appends `; rerun make final-resources-preflight` only when a known final-resource action is missing a rerun command.

**Tech Stack:** Python, FastAPI backend report builders, pytest, ruff.

---

### Task 1: Rehearsal Resource Action Normalization

**Files:**
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_rehearsal.py`
- Modify: `services/backend/src/myth_forge_api/ios_device_launch_rehearsal_readiness.py`
- Modify: `services/backend/tests/test_ios_device_launch_rehearsal.py`
- Modify: `docs/iteration-roadmap.md`

- [x] **Step 1: Write failing tests**

Add tests that generate and read saved iOS rehearsal reports with bare Meshy,
OpenAI, and deploy final-resource actions.

- [x] **Step 2: Run tests to verify RED**

Run: `cd services/backend && uv run pytest tests/test_ios_device_launch_rehearsal.py::test_ios_device_launch_rehearsal_adds_validation_to_resource_actions tests/test_ios_device_launch_rehearsal.py::test_ios_device_launch_rehearsal_readiness_adds_validation_to_saved_resource_actions -q`

Expected: FAIL because current actions keep bare resource commands.

- [x] **Step 3: Implement validation-aware normalization**

Normalize known final-resource actions after existing legacy-copy normalization
and before dedupe/limits.

- [x] **Step 4: Run focused tests**

Run the RED tests, then `cd services/backend && uv run pytest
tests/test_ios_device_launch_rehearsal.py tests/test_ios_device_evidence_bundle.py -q`.

- [x] **Step 5: Refresh local evidence**

Run `make final-local-report-refresh-local`, inspect
`services/backend/.local/ios-device-launch-rehearsal.json`, and confirm bare
resource actions are gone.

- [x] **Step 6: Run lint and diff checks**

Run `make backend-lint` and `git diff --check`.

- [x] **Step 7: Commit, merge, push, cleanup**

Commit the branch, merge to `main`, push `origin/main`, then remove this
worktree and delete the feature branch.
