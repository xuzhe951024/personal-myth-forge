# P0.625 Handoff Index Granular Live Consent Plan

## Goal

Make `final-handoff-index` align with the current granular configured evidence blocker instead of the older broad configured final acceptance command.

## Steps

1. Add a focused failing test using a saved `final-demo-launch-local` report with a granular configured 3D consent action.
2. Add live-acceptance operator action selection for the handoff index that prefers that saved granular action.
3. Run the focused test to confirm GREEN.
4. Run `test_final_handoff_index.py`, lint, and backend tests.
5. Refresh safe handoff evidence and inspect `.local/final-handoff-index.json`.
6. Merge to `main` and push.

## Safety

- Do not run `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 ...`.
- Do not call Meshy, OpenAI, Treatstock, or other external paid providers.
- Do not mutate Xcode signing, Keychain, global env, or real fulfillment state.
