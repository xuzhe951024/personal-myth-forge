# P0.66 Unified Final Resource Apply Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single project-local resource apply path for final Meshy/OpenAI/Treatstock/iOS demo values.

**Architecture:** A tracked example file documents the final resources. A POSIX shell apply script parses the file without shell evaluation, validates all required values, then delegates to existing backend and iOS writer scripts. Backend resource-template acceptance checks the new template/script/Make target.

**Tech Stack:** POSIX shell, Makefile, pytest, existing FastAPI backend test harness, SwiftPM project checks.

---

### Task 1: Unified Apply Script Tests

**Files:**
- Create: `services/backend/tests/test_final_resource_apply_script.py`
- Create later: `services/backend/scripts/apply_final_resources.sh`
- Create later: `services/backend/final-resources.env.example`
- Modify later: `services/backend/scripts/write_backend_env.sh`

- [ ] **Step 1: Write failing tests** for missing resource file, unknown key, loopback backend URL, successful redacted apply, and Treatstock provider handoff.
- [ ] **Step 2: Run focused test** with `cd services/backend && uv run pytest tests/test_final_resource_apply_script.py` and confirm failure because the script/template do not exist.
- [ ] **Step 3: Implement minimal script/template and backend writer print-provider support.**
- [ ] **Step 4: Re-run focused test and confirm pass.**

### Task 2: Resource Template Acceptance

**Files:**
- Modify: `services/backend/src/myth_forge_api/resource_template_acceptance.py`
- Modify: `services/backend/tests/test_resource_template_acceptance.py`
- Modify: `Makefile`

- [ ] **Step 1: Extend failing resource-template tests** to require `final-apply-resources`, `apply_final_resources.sh`, and `final-resources.env.example`.
- [ ] **Step 2: Run `cd services/backend && uv run pytest tests/test_resource_template_acceptance.py` and confirm failure.**
- [ ] **Step 3: Implement acceptance checks and Make target.**
- [ ] **Step 4: Re-run resource-template tests and confirm pass.**

### Task 3: Handoff Docs And Evidence

**Files:**
- Modify: `README.md`
- Modify: `docs/iteration-roadmap.md`
- Add: `docs/superpowers/verification/2026-06-06-p0.66-unified-resource-apply-regression.md`
- Add: `docs/superpowers/verification/p0.66-unified-resource-apply.html`
- Add: `docs/superpowers/verification/assets/p0.66-unified-resource-apply-390x844.png`

- [ ] **Step 1: Document how to copy the example file and run `make final-apply-resources`.**
- [ ] **Step 2: Render a 390x844 static evidence screenshot.**
- [ ] **Step 3: Run backend lint/test, Swift checks, and quick final acceptance.**
- [ ] **Step 4: Merge to `main` and rerun key checks.**
