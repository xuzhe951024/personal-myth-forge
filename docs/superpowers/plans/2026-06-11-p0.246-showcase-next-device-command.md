# P0.246 Showcase Next Device Command Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make `final-showcase-readiness.next_action.command` point to the immediate concrete iOS device command.

**Architecture:** Keep the capability-level `first_blocker.command` unchanged. Reuse the existing preferred device action source so `next_action.command` can point at `make mobile-deploy-preflight` when that action is the most useful blocked iOS device step.

**Tech Stack:** Python 3.10, backend JSON report builders, pytest, Ruff, Makefile verification targets.

---

### Task 1: Regression Test

**Files:**
- Modify: `services/backend/tests/test_final_showcase_readiness.py`

- [x] **Step 1: Write the failing test**

Add this test after `test_final_showcase_readiness_top_level_ios_blocker_prefers_preflight_detail`:

```python
def test_final_showcase_readiness_next_action_command_uses_preferred_device_action(
    tmp_path: Path,
) -> None:
    repo_root = _write_deploy_config(tmp_path)
    _write_mobile_deploy_preflight_evidence_blocked(
        repo_root,
        checks=[
            {
                "id": "development_team",
                "label": "Apple Team ID",
                "status": "blocked",
                "detail": "Missing DEVELOPMENT_TEAM",
            },
            {
                "id": "backend_base_url",
                "label": "Backend base URL",
                "status": "blocked",
                "detail": "PMF_BACKEND_BASE_URL must be iPhone-reachable",
            },
        ],
    )

    result = build_final_showcase_readiness_report(
        settings=Settings(),
        repo_root=repo_root,
    )

    blocker = result.report["first_blocker"]
    action = result.report["next_action"]

    assert blocker["id"] == "ios_deployable"
    assert blocker["command"] == "make ios-device-launch-rehearsal"
    assert action["id"] == "ios_deployable"
    assert action["command"] == "make mobile-deploy-preflight"
    assert "Next device action: make mobile-deploy-preflight" in action["detail"]
    assert "Missing DEVELOPMENT_TEAM" in action["detail"]
    assert "PMF_BACKEND_BASE_URL must be iPhone-reachable" in action["detail"]
    assert "MESHY_API_KEY" not in action["detail"]
```

- [x] **Step 2: Run test to verify RED**

Run:

```bash
cd services/backend
uv run pytest tests/test_final_showcase_readiness.py::test_final_showcase_readiness_next_action_command_uses_preferred_device_action
```

Expected: FAIL because `next_action.command` is currently `make ios-device-launch-rehearsal`.

### Task 2: Next Action Command Selection

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_showcase_readiness.py`

- [x] **Step 1: Add the concrete command helper**

Add this helper below `_preferred_device_action_hint_source`:

```python
def _preferred_device_action_command(
    device_action_bundle: dict[str, Any] | None,
) -> str:
    if not isinstance(device_action_bundle, dict):
        return ""
    action = _preferred_device_action_hint_source(device_action_bundle)
    if not isinstance(action, dict):
        return ""
    command = str(action.get("command", "")).strip()
    detail = str(action.get("evidence_detail", "")).strip()
    if not command or not detail:
        return ""
    if str(action.get("evidence_status", "")) == "missing":
        return ""
    return command
```

- [x] **Step 2: Override only the returned next action command**

Replace the body of `_next_action` after the `blocker is None` guard with:

```python
    command = str(blocker.get("command", ""))
    if blocker.get("id") == "ios_deployable":
        command = _preferred_device_action_command(device_action_bundle) or command
    return {
        "id": str(blocker.get("id", "")),
        "label": str(blocker.get("label", "")),
        "status": str(blocker.get("status", "")),
        "classification": str(blocker.get("classification", "")),
        "command": command,
        "detail": str(blocker.get("detail", "")),
        "source": "first_blocker",
    }
```

### Task 3: Roadmap and Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] **Step 1: Add the roadmap entry**

Add this bullet after P0.245:

```markdown
- P0.246 final showcase readiness next action command uses the preferred concrete device step
```

- [x] **Step 2: Run focused GREEN verification**

Run:

```bash
cd services/backend
uv run pytest tests/test_final_showcase_readiness.py::test_final_showcase_readiness_next_action_command_uses_preferred_device_action
uv run pytest tests/test_final_showcase_readiness.py
```

Expected: both commands pass.

- [x] **Step 3: Run final verification**

Run:

```bash
make backend-lint
make backend-test
git diff --check
make final-local-report-refresh
```

Expected: lint and tests pass. `make final-local-report-refresh` may exit 2 because external resources and iPhone/Xcode conditions remain missing, but `services/backend/.local/final-showcase-readiness.json` must have `next_action.command` equal to `make mobile-deploy-preflight`.

## Self-Review

- The plan covers the spec's single command selection rule.
- No placeholders, deferred decisions, or unrelated refactors.
- It preserves first blocker capability semantics while making next action executable.
