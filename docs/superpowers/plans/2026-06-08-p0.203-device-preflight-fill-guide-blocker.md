# P0.203 Device Preflight Fill Guide Blocker Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:test-driven-development.

**Goal:** Make device preflight consume the structured final resource fill guide first blocker.

---

### Task 1: Swift Contract

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/DevicePreflight.swift`

- [x] **Step 1: Add red contract assertions**

Extend `testDevicePreflightBlocksOnRequiredFinalResourceFillGuideInputs` to require first blocker copy, detail, and validation command.

- [x] **Step 2: Run red Swift contract**

Run CoreContractTests. Expected: fail because DevicePreflight still uses `requiredInputs.first`.

- [x] **Step 3: Implement first blocker detail path**

Prefer `guide.firstBlocker`; fall back to existing required input behavior.

- [x] **Step 4: Run green Swift contract**

Run CoreContractTests.

### Task 2: Source Gates

**Files:**
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [x] **Step 1: Add red source gate assertion**

Require `guide.firstBlocker` in mobile final launch readiness.

- [x] **Step 2: Implement source requirements**

Add mobile readiness and ProjectChecks source gates.

### Task 3: Verification

- [x] **Step 1: Run worktree verification**

Run:

```bash
make backend-lint
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
make final-rehearsal-local
make -n final-acceptance-configured
```

Observed: all commands passed. Backend pytest retained the existing Starlette/httpx deprecation warning; configured acceptance was dry-run only.
