# P0.543 iOS Rehearsal Fill Guide Actions Plan

## Scope

Update only `ios_device_launch_rehearsal_readiness` action normalization so iOS device rehearsal evidence uses the canonical provider fill-guide handoff path.

## Steps

1. Add RED tests in `services/backend/tests/test_ios_device_launch_rehearsal.py`.
   - Top-level readiness actions should contain the canonical provider fill-guide chain.
   - Top-level readiness actions should not contain bare provider key fill actions.
   - Sequence bounded actions should also prefer the canonical provider chain.
2. Confirm RED with focused pytest.
3. Implement by reusing `prefer_provider_fill_guide_handoff_actions()` in:
   - `_operator_actions()`
   - `_bounded_operator_actions()`
4. Confirm GREEN with focused pytest.
5. Refresh safe local evidence:
   - `make ios-device-launch-rehearsal-readiness`
   - `make final-showcase-readiness`
6. Run verification:
   - `make backend-lint`
   - `git diff --check`
   - `make backend-test`
7. Commit, fast-forward merge to `main`, push `origin/main`, and clean up the worktree.
