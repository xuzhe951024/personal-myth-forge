# P0.225 Final Resources Preflight Evidence Plan

## Goal

Persist the final resources preflight result as an ignored local evidence file
so the final handoff chain can inspect the latest resource state without
rerunning the preflight command.

## Plan

1. Add failing tests for the Make target output path:
   - `tests/test_evaluation_make_targets.py`
   - `tests/test_resource_template_acceptance.py`
   - `PersonalMythForgeMobileProjectChecks`
2. Update `Makefile` to pass
   `--output .local/final-resources-preflight.json`.
3. Update `resource_template_acceptance.py` to expose and verify the output
   path.
4. Update README handoff instructions.
5. Verify focused backend tests, Swift ProjectChecks, full backend lint/test,
   `make final-resources-preflight`, and `git diff --check`.

## Verification Log

- RED:
  - `uv run pytest tests/test_evaluation_make_targets.py
    tests/test_resource_template_acceptance.py -q`
    - failed on missing `--output .local/final-resources-preflight.json` and
      missing `final_resources_preflight.output_path`.
  - `swift run --package-path apps/mobile/ios
    PersonalMythForgeMobileProjectChecks`
    - failed on missing final resources preflight report output.
- GREEN:
  - `uv run pytest tests/test_evaluation_make_targets.py
    tests/test_resource_template_acceptance.py -q`
    - passed, `6 passed`.
  - `swift run --package-path apps/mobile/ios
    PersonalMythForgeMobileProjectChecks`
    - passed.
  - `make -n final-resources-preflight`
    - printed `--output .local/final-resources-preflight.json`.
  - `make final-resources-preflight`
    - exited `2` with missing resources and still wrote
      `services/backend/.local/final-resources-preflight.json`.
  - `make backend-lint`
    - passed.
  - `make backend-test`
    - passed, `545 passed, 1 warning`.
  - `swift run --package-path apps/mobile/ios
    PersonalMythForgeMobileCoreContractTests`
    - passed.
  - `git diff --check`
    - passed.
