# P0.69 Final Launch Unified Apply Plan

## Checklist

- [x] Add failing `final-demo-launch` tests for the unified resource apply phase.
- [x] Update launch phase construction to replace low-level writer phases with `apply_final_resources`.
- [x] Update launch commands so final demo paths advertise only `make final-apply-resources` for resource application.
- [x] Update README/roadmap handoff notes.
- [x] Add visual regression evidence for the final launch checklist.
- [x] Run focused and full verification before merging.

## Test First

Update `services/backend/tests/test_final_demo_launch.py` to require:

- `apply_final_resources` phase exists.
- old phase ids `write_backend_env` and `write_ios_deploy_config` are not launch phases.
- local/configured command lists include `make final-apply-resources`.
- local/configured command lists do not include the lower-level writer targets.

Expected first result: tests fail because the current report still exposes the old phases and commands.

## Implementation

Modify `services/backend/src/myth_forge_api/final_demo_launch.py`:

- Add a single `apply_final_resources` phase before backend server startup.
- Use configured-mode status from backend core plus iOS config.
- Use local-mode status from iOS config.
- Remove low-level writer commands from `_commands()`.

## Verification

Run:

```bash
cd services/backend && uv run pytest tests/test_final_demo_launch.py tests/test_resource_handoff.py tests/test_resource_template_acceptance.py -q
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
cd services/backend && uv run python -m myth_forge_api.cli final-demo-launch --mode local --repo-root ../.. --output .local/final-demo-launch-p0.69.json
cd services/backend && uv run python -m myth_forge_api.cli final-acceptance --profile quick --provider-mode local --repo-root ../.. --output .local/final-acceptance-p0.69.json
```
