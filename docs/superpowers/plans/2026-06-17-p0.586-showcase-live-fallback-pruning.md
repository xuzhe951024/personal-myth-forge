# P0.586 Showcase Live Fallback Pruning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Keep `final-showcase-readiness.operator_actions` from presenting configured live-provider fallback actions beside the guarded live provider consent command.

**Architecture:** Extend final showcase operator-action normalization with exact fallback roots for configured live-provider commands.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_showcase_readiness.py`

- [x] Extend the live consent gate preference test with configured-preflight and configured-evaluation fallbacks.
- [x] Confirm the old implementation keeps those fallbacks and fails.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_showcase_readiness.py`

- [x] Add exact fallback roots for configured 3D/NPC evaluation and configured-preflight unblock actions.
- [x] Filter those roots only when the guarded live consent action is present.
- [x] Preserve unrelated print/device/local resource operator actions.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.586 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused final showcase readiness tests.
- [x] Run downstream final demo launch tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect final showcase operator actions.

Safe refresh note: `final-showcase-readiness.json.operator_actions` now contains only `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make final-acceptance-configured; rerun make live-provider-evidence` for the live-provider path. Configured-preflight unblock and configured 3D/NPC evaluation fallbacks are no longer listed beside the guarded command.
