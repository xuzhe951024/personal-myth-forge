# P0.633 Configured Validation Preference Plan

## Steps

1. Add RED tests for configured bundle and final local refresh action preference.
2. Normalize env-prefixed live-provider fallback roots in configured bundle.
3. Prefer `final-configured-evidence-plan` validation over `live-provider-evidence`
   validation for granular configured live actions in final local refresh.
4. Run focused tests, related report tests, lint, full backend tests, and safe report
   refresh commands before merging and pushing.
