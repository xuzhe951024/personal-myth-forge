# P0.463 Closure Packet Device-First Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the final launch closure packet guide local iPhone operators to the device deploy blocker before provider-key blockers.

**Architecture:** Reuse the existing final showcase readiness action already present in the final acceptance section. Thread `showcase_readiness` into first-blocker selection and promote only iOS deploy/device commands. Keep the existing section-order fallback unchanged.

**Tech Stack:** Python 3.10, pytest, FastAPI backend report builders.

---

### Task 1: Add Device-First RED Coverage

**Files:**
- Modify: `services/backend/tests/test_final_launch_closure_packet.py`

- [ ] **Step 1: Update the standard missing-actions test**

In `test_final_launch_closure_packet_blocks_missing_final_actions`, expect packet-level `first_blocker.id == "final_showcase_readiness"` and the current final showcase iOS device command.

- [ ] **Step 2: Preserve resource section assertions**

Keep assertions that `sections["resource_inputs"]["status"] == "blocked"` and `sections["resource_inputs"]["first_action"]["id"] == "provide_MESHY_API_KEY"`.

- [ ] **Step 3: Verify RED**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_launch_closure_packet.py::test_final_launch_closure_packet_blocks_missing_final_actions -q
```

Expected: FAIL because current packet-level first blocker is `resource_inputs`.

### Task 2: Implement Device-First First Blocker

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_launch_closure_packet.py`

- [ ] **Step 1: Thread showcase readiness into `_first_blocker`**

Change:

```python
first_blocker = _first_blocker(sections)
```

to pass `showcase_readiness`.

- [ ] **Step 2: Add a promoted showcase blocker helper**

Add a helper that returns a blocker dict when `showcase_readiness.first_blocker.command` contains an iOS device action such as `mobile-write-deploy-config-auto`, `mobile-deploy-preflight`, or `ios-device-launch-rehearsal`.

- [ ] **Step 3: Keep fallback section-order behavior**

If the helper returns `None`, use the current section-order loop unchanged.

- [ ] **Step 4: Verify GREEN**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_launch_closure_packet.py::test_final_launch_closure_packet_blocks_missing_final_actions -q
```

Expected: PASS.

### Task 3: Preserve Non-Device Behavior

**Files:**
- Modify: `services/backend/tests/test_final_launch_closure_packet.py`
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Add fallback unit coverage**

Add a direct `_first_blocker(...)` test that passes a non-device showcase blocker and asserts the first section blocker is still returned.

- [ ] **Step 2: Update roadmap**

Add:

```markdown
- P0.463 Final launch closure packet prioritizes iPhone device blocker before provider keys
```

- [ ] **Step 3: Run focused verification**

Run:

```bash
cd services/backend && uv run pytest tests/test_final_launch_closure_packet.py -q
cd ../..
make backend-lint
git diff --check
```

Expected: all commands exit 0.
