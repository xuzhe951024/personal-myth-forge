# P0.194 Mobile First Blocker Readiness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make mobile final launch readiness acceptance and Swift project checks require the top-level final demo launch first blocker contract.

**Architecture:** Extend existing source-token gates only. No endpoint behavior or runtime logic changes are needed because P0.193 already added the backend field and mobile summary behavior.

**Tech Stack:** Python 3.10, pytest, Swift Package executable project checks.

---

### Task 1: Backend Mobile Readiness Gate

**Files:**
- Modify: `services/backend/tests/test_mobile_final_launch_readiness_acceptance.py`
- Modify: `services/backend/src/myth_forge_api/mobile_final_launch_readiness_acceptance.py`

- [ ] **Step 1: Write failing requirement id assertions**

Add assertions in `test_mobile_final_launch_readiness_acceptance_checks_endpoint_source_and_safety`:

```python
assert "model_final_demo_launch_first_blocker" in requirement_ids
assert "model_final_demo_launch_first_blocker_field" in requirement_ids
assert "mobile_summary_final_demo_launch_first_blocker_row" in requirement_ids
assert "contract_final_demo_launch_first_blocker_receipt" in requirement_ids
```

- [ ] **Step 2: Run red test**

Run:

```bash
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py::test_mobile_final_launch_readiness_acceptance_checks_endpoint_source_and_safety -q
```

Expected: fail because the new requirement ids are not registered.

- [ ] **Step 3: Add source requirements**

Add `SOURCE_REQUIREMENTS` entries:

```python
SourceRequirement("model_final_demo_launch_first_blocker", ..., PMF_MODELS_PATH, "FinalDemoLaunchFirstBlocker")
SourceRequirement("model_final_demo_launch_first_blocker_field", ..., PMF_MODELS_PATH, "firstBlocker: FinalDemoLaunchFirstBlocker?")
SourceRequirement("mobile_summary_final_demo_launch_first_blocker_row", ..., FINAL_LAUNCH_MOBILE_SUMMARY_PATH, "report.firstBlocker")
SourceRequirement("contract_final_demo_launch_first_blocker_receipt", ..., CORE_CONTRACT_TESTS_PATH, "testFinalLaunchMobileSummaryUsesTopLevelFirstBlockerReceipt")
```

- [ ] **Step 4: Update minimal source fixture**

Add those four tokens to `_write_minimal_mobile_source` so the unsafe payload test can still focus on endpoint safety.

- [ ] **Step 5: Run green backend test**

Run:

```bash
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
```

Expected: pass.

### Task 2: Swift Project Checks

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileProjectChecks/main.swift`

- [ ] **Step 1: Add failing ProjectChecks requirements**

Add checks requiring:

```swift
FinalDemoLaunchFirstBlocker
firstBlocker: FinalDemoLaunchFirstBlocker?
report.firstBlocker
testFinalLaunchMobileSummaryUsesTopLevelFirstBlockerReceipt
```

- [ ] **Step 2: Run Swift ProjectChecks**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Expected: pass once requirements are added because current source already contains the tokens.

### Task 3: Verification

- [ ] **Step 1: Run focused verification**

```bash
cd services/backend && uv run pytest tests/test_mobile_final_launch_readiness_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

- [ ] **Step 2: Run completion verification**

```bash
make backend-lint
make backend-test
make final-rehearsal-local
make -n final-acceptance-configured
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Expected: all checks pass with the existing Starlette/httpx deprecation warning only.

## Self-Review

- Spec coverage: The plan covers backend readiness acceptance, minimal source fixture, Swift ProjectChecks, and full verification.
- Placeholder scan: No TBD/TODO placeholders.
- Type consistency: Requirement ids and token strings match P0.193 source names.
