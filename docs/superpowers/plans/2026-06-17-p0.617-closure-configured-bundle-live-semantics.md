# P0.617 Closure Configured Bundle Live Semantics

## Steps

1. Inspect current saved readiness and confirm closure packet mixes live commands with local-stub metadata.
2. Add failing coverage for configured evidence bundle live action metadata source consistency.
3. Update closure packet configured bundle action projection to prefer matching live `next_action` metadata.
4. Run focused, adjacent, lint, and backend regression gates.
5. Refresh safe local evidence, merge to `main`, and push.

## Verification

- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_launch_closure_packet.py::<focused-test>`
- `PYTHONPATH=<worktree>/services/backend/src <repo>/services/backend/.venv/bin/python -m pytest services/backend/tests/test_final_launch_closure_packet.py`
- `make backend-lint`
- `make backend-test`
