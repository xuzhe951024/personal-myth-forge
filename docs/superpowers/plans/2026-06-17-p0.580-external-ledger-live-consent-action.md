# P0.580 External Ledger Live Consent Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Make `final-external-action-ledger` promote guarded live-provider consent actions instead of bare validation commands.

**Architecture:** The ledger action rows keep their existing diagnostic commands. The blocker promotion path chooses `operator_action` for live actions that require cost consent, while validation still comes from the original command.

**Tech Stack:** Python 3.10, pytest, backend report builders, Make safe report wrappers.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_external_action_ledger.py`

- [x] Add a test using valid local resources and a ready iOS deploy runbook.
- [x] Assert the first blocker is `run_live_provider_evidence`.
- [x] Assert its command and `next_action.command` are the guarded live provider action.
- [x] Assert `validation_command` remains `make live-provider-evidence`.

### Task 2: Blocker Command Normalization

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_external_action_ledger.py`

- [x] Add a helper that detects live cost actions with concrete operator actions.
- [x] Update `_blocker_command_for_action()` to return `operator_action` for those actions.
- [x] Keep all resource, device, and ordinary blocked action behavior unchanged.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.580 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused tests:

```bash
cd services/backend && uv run pytest tests/test_final_external_action_ledger.py -q
```

- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect the external ledger first blocker.

Safe refresh note: the current local report first blocker is `refresh_configured_treatstock_quote`, so the live-provider first-blocker branch is covered by the focused regression test rather than the current `.local` ordering.
