# P0.627 Configured Evidence Action Prerequisites Plan

## Steps

1. Update focused tests to require operator actions to stop at the current missing
   configured live evidence prerequisites.
2. Filter downstream configured final acceptance and demo launch operator actions
   while 3D or NPC configured evidence is still missing.
3. Run focused RED/GREEN tests for final configured evidence plan and configured
   live evidence bundle.
4. Run related test files, lint, full backend tests, refresh safe evidence, merge,
   and push.
