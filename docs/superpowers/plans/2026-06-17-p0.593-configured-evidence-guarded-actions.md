# P0.593 Configured Evidence Guarded Actions Plan

## Goal

Make the configured live evidence handoff more executable without running live provider calls.

## Steps

1. Read current git state, product docs, roadmap, and saved readiness reports.
2. Add a RED test for `final-configured-evidence-plan` when resources are ready but live provider evidence still needs consent.
3. Update the report builder so consent-required live steps use guarded `PMF_ALLOW_LIVE_PROVIDER_CALLS=1` commands.
4. Run focused and related tests.
5. Refresh safe local reports, then commit and push.

## Safety

This phase only changes generated report command text. It does not invoke Meshy, OpenAI, Treatstock, Xcode signing, Keychain, or any paid provider action.
