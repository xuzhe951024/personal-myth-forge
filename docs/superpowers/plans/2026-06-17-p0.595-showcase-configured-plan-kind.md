# P0.595 Showcase Configured Plan Kind Plan

## Goal

Make P0.594 work against the actual saved configured evidence plan report.

## Steps

1. Confirm the saved report kind from `.local/final-configured-evidence-plan.json`.
2. Change the focused test fixture to the real report kind and confirm RED.
3. Update the loader to accept the real report kind.
4. Run focused final showcase tests, lint, and safe evidence refresh.
5. Commit, merge, and push.

## Safety

This phase only changes report loading and tests. It does not run live provider
commands, write secrets, perform print orders, or touch Xcode signing/global state.
