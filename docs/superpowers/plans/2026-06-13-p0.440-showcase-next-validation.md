# P0.440 Showcase Next Validation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Show the final showcase readiness `next_action.validation_command` in the iPhone Final Launch summary.

**Architecture:** Keep the existing `FinalShowcaseReadinessNextAction` model and update only the summary row formatter. Contract coverage stays in the existing Swift contract test suite.

**Tech Stack:** Swift Package Manager, `PersonalMythForgeMobileCore`, `PersonalMythForgeMobileCoreContractTests`.

---

### Task 1: Contract Coverage

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift`

- [ ] **Step 1: Add the failing assertion**

Update `testFinalLaunchMobileSummaryShowsFinalShowcaseNextAction()` so the joined showcase readiness rows must include:

```swift
try expectContains(text, "rerun make mobile-deploy-preflight")
```

- [ ] **Step 2: Run the focused contract test binary**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: FAIL because the current `showcaseReadinessNextActionRow` does not append the validation command to the next-action row.

### Task 2: Summary Formatting

**Files:**
- Modify: `apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift`

- [ ] **Step 1: Append validation command**

In `showcaseReadinessNextActionRow(_:)`, after the detail block, append:

```swift
if let validationCommand = action.validationCommand, !validationCommand.isEmpty {
    if validationCommand.hasPrefix("rerun ") {
        parts.append("| \(validationCommand)")
    } else {
        parts.append("| rerun \(validationCommand)")
    }
}
```

- [ ] **Step 2: Run the focused contract test binary**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Expected: PASS.

### Task 3: Verification And Integration

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [ ] **Step 1: Record the roadmap line**

Append:

```markdown
- P0.440 iPhone Final Launch shows final showcase next validation command
```

- [ ] **Step 2: Run verification**

Run:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift build --package-path apps/mobile/ios
make backend-lint
git diff --check
```

Expected: all commands exit `0`.

- [ ] **Step 3: Commit**

Run:

```bash
git add apps/mobile/ios/Sources/PersonalMythForgeMobileCore/FinalLaunchMobileSummary.swift \
  apps/mobile/ios/Sources/PersonalMythForgeMobileCoreContractTests/main.swift \
  docs/iteration-roadmap.md \
  docs/superpowers/specs/2026-06-13-p0.440-showcase-next-validation-design.md \
  docs/superpowers/plans/2026-06-13-p0.440-showcase-next-validation.md
git commit -m "feat: show showcase next validation command"
```
