# P0.582 External Ledger Live First Action Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use Superpowers execution practices: TDD, worktree isolation, and verification-before-completion.

**Goal:** Make `final-external-action-ledger.operator_actions` start with the guarded live provider consent action whenever the first blocker is live provider evidence.

**Architecture:** Preserve grouped action audit data. Add a final promotion step to top-level operator action rendering after configured evidence gate normalization.

**Tech Stack:** Python 3.10, pytest, backend report builders.

---

### Task 1: RED Test

**Files:**
- Modify: `services/backend/tests/test_final_external_action_ledger.py`

- [x] Extend the live-first-blocker regression test.
- [x] Assert `operator_actions[0]` is the guarded live provider action.
- [x] Assert the configured evidence bundle gate remains after the guarded action.

### Task 2: Implementation

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_external_action_ledger.py`

- [x] Add a helper that detects live provider first blockers.
- [x] Promote the live first blocker command after existing configured-gate normalization.
- [x] Leave non-live first blocker behavior unchanged.

### Task 3: Verification

**Files:**
- Modify: `docs/iteration-roadmap.md`

- [x] Add the P0.582 roadmap line.
- [x] Run the target RED/GREEN test.
- [x] Run focused external ledger tests.
- [x] Run `make backend-lint` and `git diff --check`.
- [x] Refresh safe reports and inspect external ledger operator actions.

Safe refresh note: after refreshing with `backend-device-demo` temporarily running, `final-external-action-ledger.json.first_blocker.command`, `next_action.command`, and `operator_actions[0]` all point to `PMF_ALLOW_LIVE_PROVIDER_CALLS=1 make final-acceptance-configured; rerun make live-provider-evidence`.
