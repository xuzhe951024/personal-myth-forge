# P0.40 Resource Handoff Bundle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a sanitized `resource-handoff` CLI/report that consolidates backend provider keys, print provider slots, iOS deploy values, and final validation commands.

**Architecture:** Put report construction in a new `myth_forge_api.resource_handoff` module with small helpers for backend resources, iOS xcconfig parsing, summary counts, and safety checks. Wire the module into `myth_forge_api.cli` as a thin command; keep existing `provider-handoff` and `final-acceptance` behavior unchanged.

**Tech Stack:** Python 3.10, pytest, argparse CLI, existing `Settings` and provider readiness models, static HTML evidence, SwiftPM regression gates.

---

## File Map

- Create `services/backend/src/myth_forge_api/resource_handoff.py`: report builder and local iOS config parser.
- Modify `services/backend/src/myth_forge_api/cli.py`: add `resource-handoff` command.
- Create `services/backend/tests/test_resource_handoff.py`: unit tests for report shape, config parsing, statuses, and sanitizer behavior.
- Modify `services/backend/tests/test_cli.py`: CLI output/stdout tests.
- Modify `.env.example`: document `PRINT_PROVIDER`.
- Modify docs/evidence:
  - `README.md`
  - `apps/mobile/README.md`
  - `docs/iteration-roadmap.md`
  - `docs/superpowers/verification/p0.40-resource-handoff-bundle.html`
  - `docs/superpowers/verification/assets/p0.40-resource-handoff-bundle-390x844.png`
  - `docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
  - this plan

---

### Task 1: Resource Handoff RED Tests

**Files:**
- Create `services/backend/tests/test_resource_handoff.py`
- Modify this plan

- [ ] **Step 1: Add report shape and local default tests**

Create `services/backend/tests/test_resource_handoff.py`:

```python
import json
from pathlib import Path

from myth_forge_api.config import Settings
from myth_forge_api.resource_handoff import build_resource_handoff_report


def test_resource_handoff_reports_missing_final_core_resources(tmp_path: Path) -> None:
    repo_root = _write_deploy_config(tmp_path)

    report = build_resource_handoff_report(settings=Settings(), repo_root=repo_root)

    backend = {item["id"]: item for item in report["backend"]["items"]}
    ios = {item["id"]: item for item in report["ios"]["items"]}
    assert report["kind"] == "resource_handoff_report"
    assert report["overall_status"] == "blocked"
    assert backend["THREE_D_PROVIDER"]["status"] == "manual"
    assert backend["MESHY_API_KEY"]["status"] == "missing"
    assert backend["NPC_PROVIDER"]["status"] == "manual"
    assert backend["OPENAI_API_KEY"]["status"] == "missing"
    assert backend["PRINT_PROVIDER"]["status"] == "ready"
    assert backend["TREATSTOCK_API_KEY"]["status"] == "optional"
    assert ios["DEVELOPMENT_TEAM"]["status"] == "missing"
    assert ios["PMF_BACKEND_BASE_URL"]["status"] == "blocked"
    assert "provide MESHY_API_KEY" in " ".join(report["operator_actions"])
    assert "make mobile-deploy-preflight" in report["commands"]
```

- [ ] **Step 2: Add configured provider safety test**

Append:

```python
def test_resource_handoff_marks_core_resources_ready_without_secret_leak(
    tmp_path: Path,
) -> None:
    repo_root = _write_deploy_config(
        tmp_path,
        local_config=(
            "DEVELOPMENT_TEAM = TEAM12345\n"
            "PRODUCT_BUNDLE_IDENTIFIER = com.example.personalmythforge\n"
            "PMF_BACKEND_BASE_URL = http://192.168.1.10:8080\n"
        ),
    )
    settings = Settings(
        three_d_provider="meshy",
        meshy_api_key="sk-meshy-secret",
        npc_provider="openai",
        openai_api_key="sk-openai-secret",
        print_provider="treatstock",
        treatstock_api_key="treatstock-secret",
    )

    report = build_resource_handoff_report(settings=settings, repo_root=repo_root)
    report_text = json.dumps(report)
    backend = {item["id"]: item for item in report["backend"]["items"]}
    ios = {item["id"]: item for item in report["ios"]["items"]}

    assert backend["THREE_D_PROVIDER"]["status"] == "ready"
    assert backend["MESHY_API_KEY"]["status"] == "ready"
    assert backend["NPC_PROVIDER"]["status"] == "ready"
    assert backend["OPENAI_API_KEY"]["status"] == "ready"
    assert backend["TREATSTOCK_API_KEY"]["configured"] is True
    assert backend["TREATSTOCK_API_KEY"]["status"] == "optional"
    assert ios["DEVELOPMENT_TEAM"]["status"] == "ready"
    assert ios["PMF_BACKEND_BASE_URL"]["status"] == "ready"
    assert report["safety"]["provider_secrets_in_report"] is False
    assert "sk-meshy-secret" not in report_text
    assert "sk-openai-secret" not in report_text
    assert "treatstock-secret" not in report_text
    assert str(tmp_path) not in report_text
```

- [ ] **Step 3: Add helper for local iOS config fixtures**

Append:

```python
def _write_deploy_config(tmp_path: Path, local_config: str | None = None) -> Path:
    repo_root = tmp_path / "repo"
    config_dir = repo_root / "apps/mobile/ios/Config"
    config_dir.mkdir(parents=True)
    (config_dir / "Deployment.xcconfig").write_text(
        "\n".join(
            [
                "PRODUCT_BUNDLE_IDENTIFIER = com.personalmythforge.app",
                "DEVELOPMENT_TEAM =",
                "CODE_SIGN_STYLE = Automatic",
                "PMF_BACKEND_BASE_URL = http://127.0.0.1:8080",
                '#include? "Deployment.local.xcconfig"',
            ]
        ),
        encoding="utf-8",
    )
    if local_config is not None:
        (config_dir / "Deployment.local.xcconfig").write_text(
            local_config,
            encoding="utf-8",
        )
    return repo_root
```

- [ ] **Step 4: Verify RED**

Run:

```bash
cd services/backend && uv run pytest tests/test_resource_handoff.py -q
```

Expected: import failure because `myth_forge_api.resource_handoff` does not exist.

Record the failing command and expected import failure in
`docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
after execution.

---

### Task 2: CLI RED Tests

**Files:**
- Modify `services/backend/tests/test_cli.py`
- Modify this plan

- [ ] **Step 1: Add CLI output file test**

After `test_cli_provider_handoff_prints_json_to_stdout`, add:

```python
def test_cli_resource_handoff_writes_report(tmp_path, monkeypatch) -> None:
    output_file = tmp_path / "resource-handoff.json"
    calls = []

    def fake_report(*, repo_root=None):
        calls.append(repo_root)
        return {
            "kind": "resource_handoff_report",
            "overall_status": "blocked",
            "summary": {"ready": 1, "missing": 1, "blocked": 0, "manual": 0, "optional": 0},
            "backend": {"items": []},
            "ios": {"items": []},
            "operator_actions": [],
            "commands": [],
            "safety": {"provider_secrets_in_report": False, "local_paths_in_report": False},
        }

    monkeypatch.setattr("myth_forge_api.cli.build_resource_handoff_report", fake_report)

    exit_code = main(
        [
            "resource-handoff",
            "--repo-root",
            str(tmp_path),
            "--output",
            str(output_file),
        ]
    )

    report = json.loads(output_file.read_text(encoding="utf-8"))
    assert exit_code == 2
    assert report["kind"] == "resource_handoff_report"
    assert report["overall_status"] == "blocked"
    assert calls == [tmp_path]
```

- [ ] **Step 2: Add CLI stdout ready test**

Add:

```python
def test_cli_resource_handoff_prints_stdout_when_ready(capsys, monkeypatch) -> None:
    monkeypatch.setattr(
        "myth_forge_api.cli.build_resource_handoff_report",
        lambda repo_root=None: {
            "kind": "resource_handoff_report",
            "overall_status": "ready",
            "summary": {"ready": 2, "missing": 0, "blocked": 0, "manual": 0, "optional": 0},
            "backend": {"items": []},
            "ios": {"items": []},
            "operator_actions": [],
            "commands": [],
            "safety": {"provider_secrets_in_report": False, "local_paths_in_report": False},
        },
    )

    exit_code = main(["resource-handoff"])

    payload = json.loads(capsys.readouterr().out)
    assert exit_code == 0
    assert payload["kind"] == "resource_handoff_report"
    assert payload["overall_status"] == "ready"
```

- [ ] **Step 3: Verify RED**

Run:

```bash
cd services/backend && uv run pytest \
  tests/test_cli.py::test_cli_resource_handoff_writes_report \
  tests/test_cli.py::test_cli_resource_handoff_prints_stdout_when_ready \
  -q
```

Expected: failures because the CLI command and import are not wired.

Record the failing command and expected CLI wiring failure in
`docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
after execution.

---

### Task 3: Resource Handoff GREEN

**Files:**
- Create `services/backend/src/myth_forge_api/resource_handoff.py`
- Modify this plan

- [ ] **Step 1: Create report builder module**

Create `services/backend/src/myth_forge_api/resource_handoff.py`:

```python
from __future__ import annotations

import json
import re
from pathlib import Path
from typing import Any

from myth_forge_api.config import Settings, load_settings

Status = str

BACKEND_ENV_DESTINATION = "services/backend/.env"
IOS_DEPLOY_DESTINATION = "apps/mobile/ios/Config/Deployment.local.xcconfig"


def build_resource_handoff_report(
    *,
    settings: Settings | None = None,
    repo_root: Path | str | None = None,
) -> dict[str, Any]:
    selected_settings = settings or load_settings()
    selected_repo_root = Path(repo_root) if repo_root is not None else _default_repo_root()
    backend_items = _backend_items(selected_settings)
    ios_items = _ios_items(selected_repo_root)
    all_items = backend_items + ios_items
    summary = _summary(all_items)
    report = {
        "kind": "resource_handoff_report",
        "overall_status": _overall_status(summary),
        "summary": summary,
        "backend": {
            "destination": BACKEND_ENV_DESTINATION,
            "items": backend_items,
        },
        "ios": {
            "destination": IOS_DEPLOY_DESTINATION,
            "items": ios_items,
        },
        "operator_actions": _operator_actions(backend_items, ios_items),
        "commands": _commands(),
        "safety": {
            "provider_secrets_in_report": False,
            "local_paths_in_report": False,
            "payment_links_in_report": False,
        },
    }
    return _sanitize_report(report)
```

- [ ] **Step 2: Add backend item helpers**

Append:

```python
def _backend_items(settings: Settings) -> list[dict[str, Any]]:
    return [
        _item(
            "THREE_D_PROVIDER",
            "3D provider selection",
            BACKEND_ENV_DESTINATION,
            "real 3D generation",
            "ready" if settings.three_d_provider == "meshy" else "manual",
            configured=settings.three_d_provider == "meshy",
            missing=settings.three_d_provider != "meshy",
            notes=["Set THREE_D_PROVIDER=meshy for final real-provider acceptance."],
        ),
        _item(
            "MESHY_API_KEY",
            "Meshy API key",
            BACKEND_ENV_DESTINATION,
            "real text/image/multi-image 3D generation",
            "ready" if bool(settings.meshy_api_key) else "missing",
            configured=bool(settings.meshy_api_key),
            missing=not bool(settings.meshy_api_key),
            notes=["Backend-only key; never put it in the iOS app."],
        ),
        _item(
            "NPC_PROVIDER",
            "NPC provider selection",
            BACKEND_ENV_DESTINATION,
            "AI Agent NPC traces and ticks",
            "ready" if settings.npc_provider == "openai" else "manual",
            configured=settings.npc_provider == "openai",
            missing=settings.npc_provider != "openai",
            notes=["Set NPC_PROVIDER=openai for AI-driven NPC acceptance."],
        ),
        _item(
            "OPENAI_API_KEY",
            "OpenAI API key",
            BACKEND_ENV_DESTINATION,
            "AI Agent NPC traces and ticks",
            "ready" if bool(settings.openai_api_key) else "missing",
            configured=bool(settings.openai_api_key),
            missing=not bool(settings.openai_api_key),
            notes=["Backend-only key; mobile sees only generated NPC state."],
        ),
        _item(
            "PRINT_PROVIDER",
            "Print provider selection",
            BACKEND_ENV_DESTINATION,
            "local print quote review",
            "ready" if settings.print_provider == "local" else "optional",
            configured=bool(settings.print_provider),
            missing=False,
            notes=["Live Treatstock/Sculpteo adapters are not implemented yet."],
        ),
        _item(
            "TREATSTOCK_API_KEY",
            "Treatstock API key slot",
            BACKEND_ENV_DESTINATION,
            "future live print quote/order handoff",
            "optional",
            configured=bool(settings.treatstock_api_key),
            missing=False,
            notes=["Slot exists; current live adapter is intentionally not implemented."],
        ),
        _item(
            "SCULPTEO_API_KEY",
            "Sculpteo API key slot",
            BACKEND_ENV_DESTINATION,
            "future live print quote/order handoff",
            "optional",
            configured=bool(settings.sculpteo_api_key),
            missing=False,
            notes=["Slot exists; current live adapter is intentionally not implemented."],
        ),
        _item(
            "CAPTURE_STORAGE_DIR",
            "Capture storage directory",
            BACKEND_ENV_DESTINATION,
            "local capture media manifests",
            "ready",
            configured=True,
            missing=False,
            notes=["Default local capture storage is acceptable for development."],
        ),
    ]
```

- [ ] **Step 3: Add iOS item helpers**

Append:

```python
def _ios_items(repo_root: Path) -> list[dict[str, Any]]:
    values = _read_xcconfig_values(repo_root)
    team = values.get("DEVELOPMENT_TEAM", "")
    bundle_id = values.get("PRODUCT_BUNDLE_IDENTIFIER", "")
    backend_url = values.get("PMF_BACKEND_BASE_URL", "")
    backend_status = "ready"
    if not backend_url:
        backend_status = "missing"
    elif _is_loopback_url(backend_url):
        backend_status = "blocked"
    return [
        _item(
            "DEVELOPMENT_TEAM",
            "Apple development team",
            IOS_DEPLOY_DESTINATION,
            "iPhone signing",
            "ready" if team and team != "YOUR_TEAM_ID" else "missing",
            configured=bool(team and team != "YOUR_TEAM_ID"),
            missing=not bool(team and team != "YOUR_TEAM_ID"),
            notes=["Copy Deployment.local.xcconfig.example and fill your Apple Team ID."],
        ),
        _item(
            "PRODUCT_BUNDLE_IDENTIFIER",
            "Bundle identifier",
            IOS_DEPLOY_DESTINATION,
            "iPhone signing",
            "ready" if bool(bundle_id) else "missing",
            configured=bool(bundle_id),
            missing=not bool(bundle_id),
            notes=["Use a bundle id available to your Apple developer account."],
        ),
        _item(
            "PMF_BACKEND_BASE_URL",
            "iPhone-reachable backend URL",
            IOS_DEPLOY_DESTINATION,
            "device-to-Mac backend calls",
            backend_status,
            configured=bool(backend_url and not _is_loopback_url(backend_url)),
            missing=not bool(backend_url),
            notes=["Use a LAN URL such as http://192.168.1.10:8080, not loopback."],
        ),
        _item(
            "APPLE_SDK_LICENSE",
            "Apple SDK license/build gate",
            "local machine",
            "make mobile-xcode-build",
            "manual",
            configured=False,
            missing=False,
            notes=["Accept the Xcode/Apple SDK license outside this repo before final build."],
        ),
    ]
```

- [ ] **Step 4: Add parsing, summary, and sanitization helpers**

Append:

```python
def _read_xcconfig_values(repo_root: Path) -> dict[str, str]:
    config_dir = repo_root / "apps/mobile/ios/Config"
    values: dict[str, str] = {}
    for name in ("Deployment.xcconfig", "Deployment.local.xcconfig"):
        path = config_dir / name
        if not path.exists():
            continue
        for line in path.read_text(encoding="utf-8").splitlines():
            stripped = line.strip()
            if not stripped or stripped.startswith("//") or stripped.startswith("#"):
                continue
            if "=" not in stripped:
                continue
            key, value = stripped.split("=", 1)
            values[key.strip()] = value.strip()
    return values


def _is_loopback_url(value: str) -> bool:
    lowered = value.lower()
    return "://127.0.0.1" in lowered or "://localhost" in lowered


def _item(
    item_id: str,
    label: str,
    destination: str,
    required_for: str,
    status: Status,
    *,
    configured: bool,
    missing: bool,
    notes: list[str],
) -> dict[str, Any]:
    return {
        "id": item_id,
        "label": label,
        "destination": destination,
        "required_for": required_for,
        "status": status,
        "configured": configured,
        "missing": missing,
        "notes": notes,
    }


def _summary(items: list[dict[str, Any]]) -> dict[str, int]:
    statuses = ["ready", "missing", "blocked", "manual", "optional"]
    return {status: sum(1 for item in items if item["status"] == status) for status in statuses}


def _overall_status(summary: dict[str, int]) -> str:
    if summary["missing"] or summary["blocked"]:
        return "blocked"
    if summary["manual"] or summary["optional"]:
        return "partial"
    return "ready"


def _operator_actions(
    backend_items: list[dict[str, Any]],
    ios_items: list[dict[str, Any]],
) -> list[str]:
    actions: list[str] = []
    backend_by_id = {item["id"]: item for item in backend_items}
    ios_by_id = {item["id"]: item for item in ios_items}
    if backend_by_id["THREE_D_PROVIDER"]["status"] != "ready":
        actions.append("set THREE_D_PROVIDER=meshy")
    if backend_by_id["MESHY_API_KEY"]["status"] != "ready":
        actions.append("provide MESHY_API_KEY")
    if backend_by_id["NPC_PROVIDER"]["status"] != "ready":
        actions.append("set NPC_PROVIDER=openai")
    if backend_by_id["OPENAI_API_KEY"]["status"] != "ready":
        actions.append("provide OPENAI_API_KEY")
    if ios_by_id["DEVELOPMENT_TEAM"]["status"] != "ready":
        actions.append("provide DEVELOPMENT_TEAM in Deployment.local.xcconfig")
    if ios_by_id["PMF_BACKEND_BASE_URL"]["status"] != "ready":
        actions.append("set PMF_BACKEND_BASE_URL to an iPhone-reachable LAN URL")
    actions.append("accept the Apple SDK license outside this repository before Xcode build")
    return actions


def _commands() -> list[str]:
    return [
        "make backend-dev",
        "curl http://127.0.0.1:8080/v1/provider-readiness",
        (
            "cd services/backend && uv run python -m myth_forge_api.cli resource-handoff "
            "--output /tmp/personal-myth-forge-resource-handoff.json"
        ),
        (
            "cd services/backend && uv run python -m myth_forge_api.cli final-acceptance "
            "--profile quick --provider-mode configured --require-real-core "
            "--output /tmp/personal-myth-forge-final-acceptance.json"
        ),
        "make mobile-deploy-preflight",
        "make mobile-xcode-build",
    ]


def _default_repo_root() -> Path:
    cwd = Path.cwd()
    if cwd.name == "backend" and cwd.parent.name == "services":
        return cwd.parent.parent
    return cwd


def _sanitize_report(report: dict[str, Any]) -> dict[str, Any]:
    sanitized = json.loads(json.dumps(report))
    text = json.dumps(sanitized)
    sanitized["safety"] = {
        "provider_secrets_in_report": bool(
            re.search(r"sk-[A-Za-z0-9_-]+|api[_-]?key\s*[=:]\s*[^\s,;]+|Bearer\s+", text)
        ),
        "local_paths_in_report": str(Path.home()) in text or "/tmp/" in text,
        "payment_links_in_report": bool(re.search(r"https?://(?:pay|checkout)\.", text)),
    }
    return sanitized
```

- [ ] **Step 5: Verify GREEN**

Run:

```bash
cd services/backend && uv run pytest tests/test_resource_handoff.py -q
```

Expected: all resource handoff tests pass.

Record the passing command in
`docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
after execution.

---

### Task 4: CLI GREEN

**Files:**
- Modify `services/backend/src/myth_forge_api/cli.py`
- Modify this plan

- [ ] **Step 1: Import report builder**

In `cli.py`, add:

```python
from myth_forge_api.resource_handoff import build_resource_handoff_report
```

- [ ] **Step 2: Add parser command**

In `_build_parser()`, after `provider-handoff`, add:

```python
    resource_handoff_parser = subcommands.add_parser("resource-handoff")
    resource_handoff_parser.add_argument("--repo-root", default=None)
    resource_handoff_parser.add_argument("--output", default=None)
```

- [ ] **Step 3: Add command dispatch and helper**

In `main`, after `provider-handoff`, add:

```python
        if args.command == "resource-handoff":
            return _resource_handoff(
                repo_root=args.repo_root,
                output_path=args.output,
            )
```

After `_provider_handoff`, add:

```python
def _resource_handoff(repo_root: str | None, output_path: str | None) -> int:
    report = build_resource_handoff_report(
        repo_root=Path(repo_root) if repo_root else None,
    )
    _write_json_payload(report, output_path)
    return 0 if report["overall_status"] == "ready" else 2
```

- [ ] **Step 4: Verify GREEN**

Run:

```bash
cd services/backend && uv run pytest \
  tests/test_cli.py::test_cli_resource_handoff_writes_report \
  tests/test_cli.py::test_cli_resource_handoff_prints_stdout_when_ready \
  -q
```

Expected: both CLI tests pass.

Record the passing command in
`docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
after execution.

---

### Task 5: Documentation And Evidence

**Files:**
- Modify `.env.example`
- Modify `README.md`
- Modify `apps/mobile/README.md`
- Modify `docs/iteration-roadmap.md`
- Create `docs/superpowers/verification/p0.40-resource-handoff-bundle.html`
- Create `docs/superpowers/verification/assets/p0.40-resource-handoff-bundle-390x844.png`
- Create `docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`
- Modify this plan

- [ ] **Step 1: Update `.env.example`**

Add:

```env
PRINT_PROVIDER=local
```

under the print providers section.

- [ ] **Step 2: Update docs**

Document:

```bash
cd services/backend
uv run python -m myth_forge_api.cli resource-handoff \
  --repo-root ../.. \
  --output /tmp/personal-myth-forge-resource-handoff.json
```

Mention that the command is configuration-only, does not call live providers,
does not mutate global Xcode state, and never prints secret values.

- [ ] **Step 3: Update roadmap**

Add:

```markdown
- P0.40 consolidated final resource handoff bundle for provider keys and iOS deploy values
```

- [ ] **Step 4: Create static evidence HTML**

The 390px page should show:

- `resource-handoff`
- backend resources: Meshy/OpenAI missing until supplied
- print resources: local quote ready, live providers optional/not implemented
- iOS resources: Team ID and LAN URL missing/blocked
- Apple SDK license manual gate
- no provider secrets or local paths

- [ ] **Step 5: Render screenshot**

Use installed Chrome via Playwright and save:

```text
docs/superpowers/verification/assets/p0.40-resource-handoff-bundle-390x844.png
```

- [ ] **Step 6: Record regression evidence**

Create regression markdown with RED/GREEN, full regression, known blockers, and
evidence paths.

---

### Task 6: Full Regression, Merge, Cleanup

**Files:**
- Modify this plan

- [ ] **Step 1: Run full regression**

Run:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
cd services/backend && uv run python -m myth_forge_api.cli resource-handoff \
  --repo-root ../.. \
  --output /tmp/personal-myth-forge-resource-handoff.json
python3 - <<'PY'
import json
from pathlib import Path
report = json.loads(Path('/tmp/personal-myth-forge-resource-handoff.json').read_text())
text = json.dumps(report)
assert report['kind'] == 'resource_handoff_report'
assert report['overall_status'] == 'blocked'
assert report['safety']['provider_secrets_in_report'] is False
assert report['safety']['local_paths_in_report'] is False
assert 'sk-' not in text
print('resource-handoff-ok')
PY
cd services/backend && uv run python -m myth_forge_api.cli final-acceptance \
  --profile quick \
  --provider-mode local \
  --output /tmp/personal-myth-forge-final-acceptance.json
python3 - <<'PY'
import json
from pathlib import Path
report = json.loads(Path('/tmp/personal-myth-forge-final-acceptance.json').read_text())
checks = {check['id']: check for check in report['checks']}
assert report['summary']['passed'] == 4
assert report['summary']['blocked'] == 2
assert report['summary']['failed'] == 0
assert checks['print_quote_acceptance']['status'] == 'passed'
print('final-acceptance-ok')
PY
make mobile-deploy-preflight
make mobile-xcode-build
git diff --check HEAD
```

Expected:

- backend lint/test pass
- SwiftPM gates pass
- `resource-handoff` exits `2` in local default mode with safe blocked report
- final acceptance quick exits `2` with `passed=4`, `blocked=2`, `failed=0`
- deploy preflight exits `2` until local Team ID/LAN URL exist
- Xcode build gate exits through Apple SDK license `Error 69`
- `git diff --check HEAD` passes

Record the final command statuses and known expected blockers in
`docs/superpowers/verification/2026-06-06-p0.40-resource-handoff-bundle-regression.md`.

- [ ] **Step 2: Commit implementation branch**

Commit implementation, tests, docs, and evidence on
`codex/p0.40-resource-handoff-bundle`.

- [ ] **Step 3: Merge back to main**

Merge with `--no-ff`, rerun critical checks on main, remove the temporary
worktree, delete the branch, and mark this plan complete.

Record merge commit, main-branch verification, and worktree cleanup in the final
P0.40 turn summary.

---

## Acceptance Criteria

- `resource-handoff` writes and prints a structured JSON report.
- Report consolidates backend provider, print provider, iOS deploy, Apple SDK,
  and next-command resources.
- Default local report marks Meshy/OpenAI/iOS deploy resources not ready for
  final real-provider/device handoff.
- Configured provider settings are marked ready without leaking key values.
- Treatstock/Sculpteo remain optional/not-implemented slots.
- Report does not expose secret values, checkout URLs, or absolute local paths.
- Existing provider handoff and final acceptance behavior remain unchanged.
