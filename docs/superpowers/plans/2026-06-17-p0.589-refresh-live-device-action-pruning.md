# P0.589 Final Local Refresh Live Device Action Pruning Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans. Follow TDD and update this checklist as work completes.

**Goal:** Remove unrelated device fallback actions from `final-local-report-refresh` when the guarded live-provider consent command is already the first action.

**Architecture:** Keep action cleanup local to `final_local_report_refresh._operator_actions()` after existing dedupe and rehearsal pruning. Do not change child reports or normal device blocker behavior.

## Tasks

- [x] Read current git state, design docs, roadmap, and saved `.local` readiness reports.
- [x] Identify `final-local-report-refresh` as a repo-local handoff cleanup target.
- [x] Add RED coverage to the existing live-consent operator action test by including a nested `backend-device-demo` fallback.
- [x] Confirm RED with the focused test.
- [x] Add `_drop_device_fallbacks_when_live_consent_is_first()` after existing action pruning.
- [x] Confirm GREEN with the focused test.
- [x] Run the full `test_final_local_report_refresh.py` regression.
- [x] Run backend lint and diff checks.
- [x] Refresh local handoff/readiness reports and confirm the saved operator action list.
- [x] Merge to `main`, commit, and push.

## Verification Commands

```bash
cd services/backend
uv run pytest tests/test_final_local_report_refresh.py::test_final_local_report_refresh_operator_actions_prefer_live_consent_gate_over_provider_fallbacks -q
uv run pytest tests/test_final_local_report_refresh.py -q
```

Additional final checks:

```bash
make backend-lint
git diff --check
```
