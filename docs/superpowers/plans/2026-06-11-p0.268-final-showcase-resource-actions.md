# P0.268 Final Showcase Resource Actions Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make final showcase readiness operator actions include validation commands for nested final-resource handoff actions.

**Architecture:** Keep the change inside `final_showcase_readiness` action normalization. Preserve existing source prefixes and append `; rerun make final-resources-preflight` only for known final-resource actions that do not already include a rerun command.

**Tech Stack:** Python, FastAPI backend readiness reports, pytest, ruff.

---

### Task 1: Validation-Aware Nested Resource Actions

**Files:**
- Modify: `services/backend/src/myth_forge_api/final_showcase_readiness.py`
- Modify: `services/backend/tests/test_final_showcase_readiness.py`
- Modify: `docs/iteration-roadmap.md`

- [x] **Step 1: Write the failing test**

Add a test that builds final showcase readiness from an iOS device rehearsal
report containing prefixed Meshy/OpenAI resource actions without validation
commands.

- [x] **Step 2: Run test to verify it fails**

Run: `cd services/backend && uv run pytest tests/test_final_showcase_readiness.py::test_final_showcase_readiness_adds_validation_to_nested_resource_actions -q`

Expected: FAIL because the current actions keep the bare prefixed resource
commands.

- [x] **Step 3: Implement minimal normalization**

Add a helper in `final_showcase_readiness.py` that appends `; rerun make
final-resources-preflight` for known final-resource actions when missing.

- [x] **Step 4: Run focused tests**

Run the new test, then `cd services/backend && uv run pytest
tests/test_final_showcase_readiness.py -q`.

- [x] **Step 5: Refresh evidence and verify report output**

Run `make final-showcase-readiness` and inspect `.local/final-showcase-readiness.json`
for validation-aware nested resource actions.

- [x] **Step 6: Run lint and diff checks**

Run `make backend-lint` and `git diff --check`.

- [x] **Step 7: Commit, merge, push, cleanup**

Commit the branch, merge to `main`, push `origin/main`, and remove this worktree.
