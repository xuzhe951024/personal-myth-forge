# P0.604 Top-Level Live Action Order

## Goal

Keep final operator handoff order aligned with the configured plan once local device
evidence is ready and live provider consent is the next blocker.

## Steps

1. Inspect current `.local` reports and source for closure packet and external action
   ledger live action routing.
2. Add RED coverage for 3D-first configured provider consent at both top-level reports.
3. Preserve configured bundle operator actions in closure packet action generation.
4. Let external action ledger read configured live evidence bundle via delayed import
   and prefer its first operator action.
5. Run focused tests, lint, full backend tests, safe refresh, commit, merge, and push.

## Safety

This phase only changes read-only handoff report generation and tests. It must not run
live provider commands, write secrets, place print orders, or touch Xcode signing.
