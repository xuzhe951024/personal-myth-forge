# P0.22 NPC Agent Ticks Regression Evidence

Date: 2026-06-06

Scope:
- Add a stateless `POST /v1/npc-ticks` backend API.
- Add deterministic local NPC tick generation and world arbitration.
- Add Swift `NPCAgentTick` model/client support.
- Add the mobile `NPCTickView` panel for the next NPC village tick.
- Keep live LLM ticks, persistence, Unity movement, and voice out of this slice.

## TDD Evidence

Backend RED:
- `cd services/backend && uv run pytest tests/test_npc_ticks.py -q`
- Failed during collection with `ImportError: cannot import name 'NPCAgentTickRequest'`.

Backend GREEN:
- `cd services/backend && uv run pytest tests/test_npc_ticks.py tests/test_api_contract.py -q`
- Result: 13 passed, 1 existing Starlette/httpx warning.

Swift core RED:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`
- Failed because `NPCAgentTick` and `createNPCAgentTick` were not in scope.

Swift core GREEN:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`
- Result: passed.

SwiftUI project RED:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`
- Failed because `apps/mobile/ios/App/NPCTickView.swift` was missing.

SwiftUI project GREEN:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`
- Result: passed.
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck`
- Result: passed.

## Visual Evidence

Static evidence page:
- `docs/superpowers/verification/p0.22-npc-agent-ticks.html`

Captured screenshot:
- `docs/superpowers/verification/assets/p0.22-npc-agent-ticks-390x844.png`
- `file docs/superpowers/verification/assets/p0.22-npc-agent-ticks-390x844.png`
- Result: PNG image data, 390 x 844, 8-bit/color RGB, non-interlaced.

Manual visual check:
- Shows `local_tick_runtime`.
- Shows Mara, Ior, and Senn trace cards.
- Shows accepted actions, visible world changes, and one rejected unsafe action.

## Full Verification

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`

Result:
- Passed.

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`

Result:
- Passed.

Command:
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck`

Result:
- Passed.

Command:
- `make backend-lint`

Result:
- `All checks passed!`

Command:
- `make backend-test`

Result:
- 91 passed, 1 existing Starlette/httpx deprecation warning.

Command:
- `make mobile-xcode-build`

Result:
- Stopped at the Apple SDK license gate:
  `You have not agreed to the Xcode license agreements.`
- `make: *** [mobile-xcode-build] Error 69`
- No `sudo xcodebuild -license` command was run.

Command:
- `git diff --check HEAD`

Result:
- Passed with no output.

## Safety Checks

- The local tick runtime summarizes recent events by count and does not echo raw event text.
- API/mobile outputs do not include data URIs or provider keys.
- Mobile project checks scan app/core paths for secret markers including `sk-`.
- The tick API is explicit and stateless; it does not start timers, background loops, or autonomous persistence.
