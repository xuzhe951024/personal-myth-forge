# P0.118 Scene Load Proof Visual Regression

## Scope

P0.118 adds a local visual regression gate and makes the iPhone SceneKit load-proof preview the first required artifact in that gate.

## Visual Artifact

- HTML: `docs/superpowers/verification/p0.118-scene-load-proof.html`
- PNG: `docs/superpowers/verification/assets/p0.118-scene-load-proof-390x844.png`
- Viewport: `390x844`

## Behavior Covered

- `SceneKit load proof: Loaded` is visible.
- `SceneKit load proof: Conversion needed` is visible.
- `SceneKit load proof: Failed` is visible.
- `Retry Scene Load` is visible.
- Artifact preview and NPC ritual markers fit inside the mobile viewport.
- No provider keys, local paths, checkout fields, or raw media data are present.

## Verification

Run:

```bash
make visual-regression
file docs/superpowers/verification/assets/p0.118-scene-load-proof-390x844.png
sips -g pixelWidth -g pixelHeight docs/superpowers/verification/assets/p0.118-scene-load-proof-390x844.png
```

Expected:

- `make visual-regression` exits `0`.
- PNG dimensions are `390 x 844`.
- The static page shows loaded, conversion-needed, and failed SceneKit proof rows without text overlap or right-edge clipping.

Observed on 2026-06-07:

- `make visual-regression`: passed with `1` artifact passed and `0` failed.
- `file`: PNG image data, `390 x 844`, RGB, non-interlaced.
- `sips`: `pixelWidth: 390`, `pixelHeight: 844`.
