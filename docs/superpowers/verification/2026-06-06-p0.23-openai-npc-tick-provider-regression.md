# P0.23 OpenAI NPC Tick Provider Regression Evidence

Date: 2026-06-06

Scope:
- Add `OpenAINPCTickRuntime` for structured stateless NPC ticks.
- Route `POST /v1/npc-ticks` through `build_npc_tick_runtime`.
- Reuse `NPC_PROVIDER=openai`, `OPENAI_API_KEY`, `OPENAI_NPC_MODEL`, and
  `OPENAI_API_BASE_URL`.
- Keep provider keys backend-only.
- Preserve the local deterministic no-key tick runtime.

## TDD Evidence

OpenAI tick runtime RED:
- `cd services/backend && uv run pytest tests/test_npc_ticks.py -q`
- Result: failed during collection with
  `ImportError: cannot import name 'OpenAINPCTickOutput'`.

OpenAI tick runtime GREEN:
- `cd services/backend && uv run pytest tests/test_npc_ticks.py -q`
- Result: 10 passed, 1 existing Starlette/httpx warning.

Factory/API/readiness RED:
- `cd services/backend && uv run pytest tests/test_provider_factory.py tests/test_api_contract.py tests/test_provider_readiness.py -q`
- Result: failed during collection with
  `ImportError: cannot import name 'build_npc_tick_runtime'`.

Factory/API/readiness GREEN:
- `cd services/backend && uv run pytest tests/test_provider_factory.py tests/test_api_contract.py tests/test_provider_readiness.py -q`
- Result: 22 passed, 1 existing Starlette/httpx warning.

## Visual Evidence

Static evidence page:
- `docs/superpowers/verification/p0.23-openai-npc-tick-provider.html`

Captured screenshot:
- `docs/superpowers/verification/assets/p0.23-openai-npc-tick-provider-390x844.png`
- `file docs/superpowers/verification/assets/p0.23-openai-npc-tick-provider-390x844.png`
- Result: PNG image data, 390 x 844, 8-bit/color RGB, non-interlaced.

Manual visual check:
- Shows OpenAI NPC provider readiness with `structured_agent_ticks`.
- Shows `Next Village Tick`.
- Shows `openai_tick_structured_runtime`.
- Shows three NPC trace cards.
- Shows accepted and rejected world arbitration sections.

## Full Worktree Verification

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`

Result:
- Passed.

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`

Result:
- Passed.

Command:
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck`

Result:
- Passed.

Command:
- `make backend-lint`

Result:
- `All checks passed!`

Command:
- `make backend-test`

Result:
- 101 passed, 1 existing Starlette/httpx deprecation warning.

Command:
- `make mobile-xcode-build`

Result:
- Stopped at the Apple SDK license gate:
  `You have not agreed to the Xcode license agreements.`
- `make: *** [mobile-xcode-build] Error 69`
- No `sudo xcodebuild -license` command was run.

Command:
- `git diff --check HEAD`

Result:
- Passed with no output.

## Safety Checks

- The OpenAI tick prompt uses approved myth session fields and recent event count.
- Raw `recent_events` text is not sent to the OpenAI prompt in P0.23.
- The prompt does not include generated asset data URIs or local capture paths.
- API/mobile outputs do not include provider keys.
- OpenAI-produced actions are still passed through world arbitration.
- The iOS app still has no provider key handling.
