# P0.84 Forge Action Gate Regression

Date: 2026-06-06
Branch: `codex/p0.84-forge-action-gate`

## Scope

P0.84 connects the visible Forge readiness state to the primary `Forge Myth` action. The button now follows a mobile-core `ForgeActionGate` instead of independently checking only media and context readiness.

## TDD Evidence

- Baseline before implementation:
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
  - `uv run pytest tests/test_ios_showcase_acceptance.py -q` passed.
- RED:
  - Added Forge action gate contract tests first.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` failed because `ForgeActionGateBuilder` was not defined.
- GREEN:
  - Added `ForgeActionGate` and `ForgeActionGateBuilder`.
  - Focused Swift core contract tests passed.
  - SwiftUI app compile gate passed after fixing a SwiftUI `foregroundStyle` type mismatch.

## Verification

- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck` passed.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
- `uv run pytest tests/test_ios_showcase_acceptance.py -q` passed.
- `make backend-lint` passed.
- `make backend-test` passed: 267 passed, 1 existing Starlette/httpx deprecation warning.
- `uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../..` returned exit code 2 with expected local blockers: 12 passed, 2 blocked, 0 failed.

## Visual Evidence

- HTML source: `docs/superpowers/verification/p0.84-forge-action-gate.html`
- Screenshot: `docs/superpowers/verification/assets/p0.84-forge-action-gate-390x844.png`
- Visual QA result: the 390x844 image shows `Forge Readiness`, `CHECK`, `needs_attention`, missing `MESHY_API_KEY`, a disabled `Forge Myth` button, and a visible next-action reason without text overlap or right-edge clipping.

## Known Blockers

- `mobile_deploy_preflight` is still expected to block until `Deployment.local.xcconfig` has a real `DEVELOPMENT_TEAM` and a non-loopback iPhone-reachable backend URL.
- `mobile_xcode_build` is still expected to block until the local Apple SDK/Xcode license is accepted. P0.84 does not run global Xcode or signing commands.
