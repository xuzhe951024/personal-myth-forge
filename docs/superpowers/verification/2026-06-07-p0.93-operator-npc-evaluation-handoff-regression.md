# P0.93 Operator NPC Evaluation Handoff Visual Regression

## Commands

Focused backend and source gates:

```bash
cd services/backend
uv run pytest tests/test_final_operator_handoff.py tests/test_final_demo_launch.py tests/test_ios_showcase_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Full local regression:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
cd services/backend
uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../.. --output .local/final-acceptance-p0.93-worktree.json
```

Rendered with Playwright using the locally installed Google Chrome executable:

```text
docs/superpowers/verification/p0.93-operator-npc-evaluation-handoff.html
docs/superpowers/verification/assets/p0.93-operator-npc-evaluation-handoff-390x844.png
```

## Result

- Final Operator Handoff step id: `npc_agent_evaluation`
- Ready path: Mac-side operator sees `Run NPC Agent evaluation` before iOS deploy preflight
- Missing path: handoff next actions include the saved NPC evaluation readiness command
- Blocked path: unsafe NPC evaluation detail is redacted as `[withheld]`
- Final launch path: handoff consumes the already-built NPC Agent evaluation readiness report

## Visual Inspection

The 390x844 screenshot shows the final operator handoff with `Run NPC Agent
evaluation` between local final acceptance and iOS deploy preflight. The NPC
evaluation receipt remains visible below the command list, and text fits in the
mobile viewport without overlapping controls, clipped labels, provider secrets,
local absolute paths, raw private markers, media payloads, payment links, Xcode
changes, or global configuration mutations.

No OpenAI, Meshy, Treatstock, Xcode, signing, deployment, network, or global
configuration command was executed for this visual artifact.
