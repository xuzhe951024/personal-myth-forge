# P0.92 Demo Script NPC Readiness Visual Regression

## Commands

Swift contract and source gates:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
cd services/backend
uv run pytest tests/test_ios_showcase_acceptance.py -q
```

Rendered with Playwright using the locally installed Google Chrome executable:

```text
docs/superpowers/verification/p0.92-demo-script-npc-readiness.html
docs/superpowers/verification/assets/p0.92-demo-script-npc-readiness-390x844.png
```

## Result

- Demo Script step id: `npc_evaluation`
- Ready path: NPC evaluation completes before `final_launch`
- Missing path: Autopilot returns non-executable `Check NPC Eval`
- Blocked path: Autopilot returns blocked `Check NPC Eval`
- Sanitization: unsafe blocker detail is redacted as `[withheld]`

## Visual Inspection

The 390x844 screenshot shows the iPhone Demo Script with `NPC Evaluation` between
`Check Resources` and `Final Launch`, plus a `Check NPC Eval` blocked button
state. Text fits in the mobile viewport without overlapping controls, clipped
step labels, provider secrets, local absolute paths, raw private markers, media
payloads, or payment links.

No OpenAI, Meshy, Treatstock, Xcode, signing, deployment, network, or global
configuration command was executed for this visual artifact.
