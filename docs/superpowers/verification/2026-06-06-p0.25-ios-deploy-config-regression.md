# P0.25 iOS Deploy Config Regression Evidence

Date: 2026-06-06

Scope:
- Add checked-in iOS deployment defaults in `Deployment.xcconfig`.
- Keep local signing/backend values in ignored `Deployment.local.xcconfig`.
- Route `PMFBackendBaseURL` through `$(PMF_BACKEND_BASE_URL)`.
- Add `make mobile-deploy-preflight`.
- Avoid global Xcode mutation, provider secrets in iOS, and committed Apple
  signing values.

## Baseline Evidence

Before Task 2 implementation in the P0.25 worktree:

- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`
  failed with missing `apps/mobile/ios/scripts/deploy_preflight.sh`.

## TDD Evidence

Project check RED:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`
- Result: failed because `deploy_preflight.sh` did not exist.

Project check GREEN:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`
- Result: passed.

Preflight behavior:
- `apps/mobile/ios/scripts/deploy_preflight.sh`
- Result: exited `2` without local config, reporting missing `DEVELOPMENT_TEAM`
  and a loopback backend URL.

Temporary local config behavior:
- Copied `Deployment.local.xcconfig.example` to the ignored local config.
- `apps/mobile/ios/scripts/deploy_preflight.sh`
- Result: exited `0`, printed the example bundle id and LAN backend URL.
- Removed the ignored local config after the check.

## Visual Evidence

Static evidence page:
- `docs/superpowers/verification/p0.25-ios-deploy-config.html`

Captured screenshot:
- `docs/superpowers/verification/assets/p0.25-ios-deploy-config-390x844.png`
- `file docs/superpowers/verification/assets/p0.25-ios-deploy-config-390x844.png`
- Result: PNG image data, 390 x 844, 8-bit/color RGB, non-interlaced.

Manual visual check:
- Shows `iOS Deploy Config`.
- Shows preflight `exit 2` and configured pass states.
- Shows tracked shared config and ignored local override.
- Shows non-loopback device URL requirement.
- Shows no global Xcode mutation policy.

## Full Worktree Verification

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`

Result:
- Passed.

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`

Result:
- Passed.

Command:
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck`

Result:
- Passed.

Command:
- `make backend-lint`

Result:
- `All checks passed!`

Command:
- `make backend-test`

Result:
- 105 passed, 1 existing Starlette/httpx deprecation warning.

Command:
- `make mobile-deploy-preflight`

Result:
- Expected environment gate: exited `2` because `Deployment.local.xcconfig` is
  absent, `DEVELOPMENT_TEAM` is missing, and the default backend URL is loopback.

Command:
- `make mobile-xcode-build`

Result:
- Expected environment gate: stopped at the Apple SDK license prompt.
- Output included `make: *** [mobile-xcode-build] Error 69`.
- No `sudo xcodebuild -license` command was run.

Command:
- `git diff --check HEAD`

Result:
- Passed with no output.

## Safety Checks

- `Deployment.local.xcconfig` is ignored by git.
- No real Apple Team ID, provisioning profile, provider key, bearer token, or
  local device URL is committed.
- The deploy preflight does not call `xcode-select`, accept licenses, or mutate
  global Xcode state.
- Provider keys remain backend-only.
