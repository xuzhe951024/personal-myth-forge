# P0.99 Forge Progress Receipt Visual Regression

## Artifact

- HTML: `docs/superpowers/verification/p0.99-forge-progress-receipt.html`
- PNG: `docs/superpowers/verification/assets/p0.99-forge-progress-receipt-390x844.png`
- Viewport: 390x844

## Result

The static iPhone visual shows the new `Forge Progress` receipt in both in-flight
and ready states:

- `RUN` with capture upload, myth session, 3D generation, and NPC Agent pending
  rows
- `READY` with local 3D provider route and NPC Agent runtime rows
- privacy notes for raw media, provider keys, local paths, and approved capsule
  summaries

Manual inspection confirms the rows are readable at 390x844. Badges stay aligned
in the headers, details wrap without overlap, and the lower provider/route metric
tiles keep stable dimensions.

## Chrome Capture Notes

Chrome wrote the PNG but did not exit on its own. The main Chrome process using
the temporary `/tmp/pmf-chrome-p0.99.*` profile was terminated after the PNG was
written. The temporary profile directory was removed, and no matching
`/tmp/pmf-chrome-p0.99.*` directory remained after capture.
