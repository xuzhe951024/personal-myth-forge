# P0.116 Final Handoff Source Freshness Regression

P0.116 adds source freshness to the final handoff index and surfaces stale
indexed local evidence through the saved iOS device launch rehearsal readiness
shown on the iPhone.

## Focused Verification

Backend source freshness:

```bash
cd services/backend && uv run pytest tests/test_final_handoff_index.py -q
```

Result:

```text
6 passed, 1 known Starlette/httpx deprecation warning
```

Rehearsal and final launch propagation:

```bash
cd services/backend && uv run pytest tests/test_ios_device_launch_rehearsal.py tests/test_final_demo_launch.py -q
```

Result:

```text
21 passed, 1 known Starlette/httpx deprecation warning
```

Swift decode and mobile summary:

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Result:

```text
PersonalMythForgeMobileCoreContractTests passed
```

Source gates:

```bash
cd services/backend && uv run pytest tests/test_resource_template_acceptance.py tests/test_ios_showcase_acceptance.py -q
```

Result:

```text
18 passed
```

## Visual Evidence

- HTML fixture:
  `docs/superpowers/verification/p0.116-final-handoff-source-freshness.html`
- PNG evidence:
  `docs/superpowers/verification/assets/p0.116-final-handoff-source-freshness-390x844.png`

The local Node package could import Playwright, but the required Chromium binary
was not installed in the user cache. To avoid global browser installation or
machine-state mutation, the PNG evidence was generated with local Python/Pillow.
The rendered evidence shows:

- `iOS launch rehearsal blocked`
- `Freshness: fresh_report`
- `Source freshness: 4 fresh, 1 stale, 0 unknown.`
- `final_handoff_index ... stale_report`
- no provider secrets, raw media, payment links, or local paths
