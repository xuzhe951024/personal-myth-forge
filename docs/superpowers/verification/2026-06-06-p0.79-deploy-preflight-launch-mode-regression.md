# P0.79 Deploy Preflight Launch Mode Regression

Date: 2026-06-06

Scope:

- `mobile-deploy-preflight` now reads `PMF_FINAL_LAUNCH_MODE` from the merged iOS deployment config.
- Allowed values are `local` and `configured`.
- Invalid mode values block with exit 2 before the backend health probe.
- Successful preflight output includes `Final launch mode: <mode>`.

Focused verification:

```bash
cd services/backend
uv run pytest tests/test_ios_deploy_preflight_script.py tests/test_ios_deploy_config_writer_script.py -q
cd ../..
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Safety notes:

- No Xcode command, signing mutation, Apple license command, provider API call, provider secret read, or global configuration mutation is introduced.
- The script still performs only the existing project-local config checks and backend `/health` probe.

Static evidence:

```text
docs/superpowers/verification/p0.79-deploy-preflight-launch-mode.html
docs/superpowers/verification/assets/p0.79-deploy-preflight-launch-mode-390x844.png
```
