# P0.215 Final Demo Launch Local Alias Regression

This artifact captures the explicit local final demo launch command as a static 390x844 iPhone proof.

- Viewport: 390x844.
- Evidence: `make final-demo-launch-local` is the canonical local command for regenerating `final-demo-launch-local.json`.
- Compatibility: `make final-demo-launch` remains available as an alias.
- Safety: the command writes only the ignored local report and does not call providers, run Xcode, sign builds, or mutate global machine state.
