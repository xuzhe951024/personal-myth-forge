# P0.224 Xcode Evidence Device Action Visual Regression

This static 390x844 iPhone evidence card covers the Final Launch Status panel after final showcase readiness maps `mobile-xcode-build-evidence` into the Xcode/signing device action.

The card verifies that the mobile summary can show `Xcode Evidence`, `evidence blocked`, `make mobile-xcode-build`, `make mobile-xcode-build-evidence`, and `Apple SDK license agreement is not accepted.` without running Xcode, accepting Apple licenses, writing signing config, calling providers, exposing secrets, or mutating global machine state.
