# P0.457 Demo Script External Actions Visual Regression

This static 390x844 iPhone evidence card covers the Demo Script after P0.455 added the `external_actions` checkpoint and P0.456 added acceptance coverage.

The card verifies:

- `External Actions` appears before `Final Launch`.
- Autopilot shows `Check Actions`.
- The operator can see the `backend-device-demo` local handoff.
- The copy remains local-only: no providers, Xcode, signing, payment, or global mutation.

The artifact is static verification evidence only. It does not run live providers, mutate local config, execute Xcode, or expose raw private context.
