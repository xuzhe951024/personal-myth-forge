# P0.94 iOS Deploy Runbook Visual Regression

## Commands

Focused backend and source gates:

```bash
cd services/backend
uv run pytest tests/test_ios_deploy_runbook.py tests/test_final_demo_launch.py tests/test_final_operator_handoff.py tests/test_ios_showcase_acceptance.py -q
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Full local regression:

```bash
make backend-lint
make backend-test
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
cd services/backend
uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../.. --output .local/final-acceptance-p0.94-worktree.json
```

Rendered with the locally installed Google Chrome executable in headless mode:

```text
docs/superpowers/verification/p0.94-ios-deploy-runbook.html
docs/superpowers/verification/assets/p0.94-ios-deploy-runbook-390x844.png
```

## Result

- New report kind: `ios_deploy_runbook_report`
- New CLI/Make path: `ios-deploy-runbook` and `make ios-deploy-runbook`
- Final launch embeds `ios_deploy_runbook`
- Final operator handoff includes `ios_deploy_runbook` before iOS deploy preflight
- Safety remains read-only: no commands, provider calls, Xcode changes, signing,
  keychain operations, or global config mutation

## Visual Inspection

The 390x844 screenshot shows deploy input slots, ordered Mac-side commands,
manual Xcode build gate, and a safety receipt. Text fits in the mobile viewport
without overlapping controls, clipped labels, provider secrets, local absolute
paths, raw private markers, media payloads, payment links, Xcode changes, or
global configuration mutations.
