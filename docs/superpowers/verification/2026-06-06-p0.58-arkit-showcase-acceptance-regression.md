# P0.58 ARKit Showcase Acceptance Regression

Date: 2026-06-06

## Scope

P0.58 adds `arkit_scan_package` to the source-only iOS showcase acceptance gate
and nested quick final acceptance report.

## Red-Green Evidence

- RED: `cd services/backend && uv run pytest tests/test_ios_showcase_acceptance.py tests/test_final_acceptance.py -q`
  failed with 3 iOS showcase failures because the runner still reported 14
  features and did not fail when `ARKitScanPackageBuilder.swift` was removed.
- GREEN: the same focused command passed after adding the
  `arkit_scan_package` `FeatureRequirement`.

## Implemented Checks

`ios_showcase_acceptance` now requires:

- `ARKitScanPackageBuilder.swift` with `ARKitScanPackageBuilder`,
  `maximumReferenceImages = 11`, and
  `CaptureMediaSelection(mode: .arkitScan`.
- `ForgeRootView.swift` with `arkitScanPackageSelection` and
  `ARKitScanPackageBuilder.selection`.
- Mobile core contract tests with
  `testARKitScanPackageBuilderBuildsReadySelection`.

## Visual Evidence

```text
docs/superpowers/verification/p0.58-arkit-showcase-acceptance.html
docs/superpowers/verification/assets/p0.58-arkit-showcase-acceptance-390x844.png
```

## Verification

Full verification:

```bash
make backend-lint
# All checks passed!

make backend-test
# 208 passed, 1 warning

swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
# PersonalMythForgeMobileProjectChecks passed

swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
# PersonalMythForgeMobileCoreContractTests passed

swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
# Build of product 'PersonalMythForgeMobileAppCompileCheck' complete

cd services/backend
uv run python -m myth_forge_api.cli final-acceptance --profile quick --provider-mode local --repo-root ../.. --output /tmp/personal-myth-forge-final-acceptance-p0.58.json
```

Quick final acceptance result: exit `2`, `overall_status=blocked`,
`passed=9`, `blocked=2`, `failed=0`, `skipped=0`.
`ios_showcase_acceptance` passed with summary `passed=15`, `failed=0` and
included `arkit_scan_package`. Remaining blockers are local iOS deploy config
and Apple SDK/Xcode license state.
