# P0.83 Forge Readiness Summary Regression

Date: 2026-06-06
Branch: `codex/p0.83-forge-readiness-summary`

## Scope

P0.83 adds a mobile Forge readiness summary before the `Forge Myth` action. The panel consolidates capture readiness, context capsule approval, provider readiness, and NPC Agent mode while keeping provider keys, raw media paths, and raw private source text out of the UI.

## TDD Evidence

- Baseline before implementation:
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
  - `uv run pytest tests/test_ios_showcase_acceptance.py -q` passed.
- RED:
  - Added Forge readiness contract tests first.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` failed because `ForgeReadinessSummaryBuilder` was not defined.
- GREEN:
  - Added `ForgeReadinessSummaryBuilder`.
  - Wired `CaptureFormView` and `ForgeRootView`.
  - Focused Swift contract tests passed.

## Verification

- `make backend-lint` passed.
- `make backend-test` passed: 267 passed, 1 existing Starlette/httpx deprecation warning.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck` passed.
- `uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../..` returned exit code 2 with expected local blockers: 12 passed, 2 blocked, 0 failed.

## Visual Evidence

- HTML source: `docs/superpowers/verification/p0.83-forge-readiness-summary.html`
- Screenshot: `docs/superpowers/verification/assets/p0.83-forge-readiness-summary-390x844.png`
- Visual QA result: the 390x844 image shows `Forge Readiness`, `READY`, `local_demo`, provider/privacy rows, and the `Forge Myth` button without text overlap or right-edge clipping.

## Known Blockers

- `mobile_deploy_preflight` remains blocked until `Deployment.local.xcconfig` has a real `DEVELOPMENT_TEAM` and a non-loopback iPhone-reachable backend URL.
- `mobile_xcode_build` remains blocked because the local Apple SDK/Xcode license has not been accepted. No global Xcode or signing commands were run.
