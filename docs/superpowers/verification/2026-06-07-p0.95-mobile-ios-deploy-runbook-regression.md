# P0.95 Mobile iOS Deploy Runbook Regression

## Scope

P0.95 adds iPhone Final Launch decoding and rendering for the P0.94
`ios_deploy_runbook` report.

## Visual Evidence

- HTML: `docs/superpowers/verification/p0.95-mobile-ios-deploy-runbook.html`
- Screenshot: `docs/superpowers/verification/assets/p0.95-mobile-ios-deploy-runbook-390x844.png`
- Viewport: 390x844

## Checks

- `iOS Deploy Runbook` appears between `NPC Evaluation` and `Next`.
- `Deploy Commands` shows Mac-side commands as read-only rows.
- `Deploy Safety` shows false values for command execution, provider calls,
  global mutation, secrets, raw media, payment links, and local paths.
- Screenshot has no visible overlap or clipped text in the mobile viewport.

## Safety

The visual artifact is static HTML. It does not execute shell commands, call
providers, mutate global state, or expose secrets.
