# P0.46 Resource Template Acceptance Regression

## Scope

P0.46 adds a source-only acceptance gate that verifies final resource templates
are complete and safe to commit.

## Checks

Targeted tests:

```bash
cd services/backend && uv run pytest tests/test_resource_template_acceptance.py tests/test_resource_handoff.py tests/test_final_acceptance.py tests/test_cli.py -q
```

Result:

```text
37 passed
```

Resource template CLI:

```bash
cd services/backend && uv run python -m myth_forge_api.cli resource-template-acceptance --repo-root ../.. --output /tmp/personal-myth-forge-resource-template-acceptance.json
```

Result:

```text
exit 0
summary: passed=7 failed=0
```

Quick final acceptance:

```bash
cd services/backend && uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../.. --output /tmp/personal-myth-forge-final-acceptance-p0.46.json
```

Result:

```text
exit 2
summary: passed=7 blocked=2 failed=0 skipped=0
```

Expected blockers:

- `mobile_deploy_preflight`: blocked by local iOS deploy config.
- `mobile_xcode_build`: blocked by Apple SDK license.

Backend lint:

```bash
make backend-lint
```

Result:

```text
All checks passed!
```

Backend tests:

```bash
make backend-test
```

Result:

```text
182 passed, 1 warning
```

The warning is the existing FastAPI/Starlette `TestClient` deprecation warning.

## Template Coverage

Backend template slots verified:

- `THREE_D_PROVIDER`
- `MESHY_API_BASE_URL`
- `MESHY_POLL_INTERVAL_SECONDS`
- `MESHY_MAX_WAIT_SECONDS`
- `MESHY_API_KEY`
- `NPC_PROVIDER`
- `OPENAI_NPC_MODEL`
- `OPENAI_API_BASE_URL`
- `OPENAI_API_KEY`
- `PRINT_PROVIDER`
- `TREATSTOCK_API_KEY`
- `SCULPTEO_API_KEY`
- `CAPTURE_STORAGE_DIR`
- `MYTH_SESSION_STORAGE_DIR`

iPhone deployment template slots verified:

- `DEVELOPMENT_TEAM`
- `PRODUCT_BUNDLE_IDENTIFIER`
- `PMF_BACKEND_BASE_URL`

Ignored local destinations verified:

- `.env`
- `services/backend/.env`
- `apps/mobile/ios/Config/Deployment.local.xcconfig`
- `services/backend/.local/`

## Safety

The resource template acceptance report confirms:

- No provider secrets in templates or report.
- No raw media in templates or report.
- No local absolute paths in templates or report.
- No payment links in templates or report.
- No provider calls.
- No global mutation.
