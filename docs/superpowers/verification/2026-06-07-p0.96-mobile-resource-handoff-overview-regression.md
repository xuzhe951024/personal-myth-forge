# P0.96 Mobile Resource Handoff Overview Regression

## Scope

P0.96 adds iPhone Final Launch decoding and rendering for the backend
`resource_report` payload.

## Visual Evidence

- HTML: `docs/superpowers/verification/p0.96-mobile-resource-handoff-overview.html`
- Screenshot: `docs/superpowers/verification/assets/p0.96-mobile-resource-handoff-overview-390x844.png`
- Viewport: 390x844

## Checks

- `Resource Handoff` appears after `Resource Checklist`.
- `Backend Resources` shows backend provider/key handoff slots as read-only
  rows from the backend report.
- `iOS Resources` shows device deploy slots as read-only rows from the backend
  report.
- Existing `iOS Deploy Runbook` remains visible below the resource overview.
- Screenshot has no visible overlap or clipped text in the mobile viewport.

## Safety

The visual artifact is static HTML. It does not execute shell commands, call
providers, mutate global state, or expose secret values.
