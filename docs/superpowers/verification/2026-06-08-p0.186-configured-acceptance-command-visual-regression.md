# P0.186 Configured Acceptance Command Visual Regression

P0.186 adds a default visual regression artifact for the iPhone Final Launch
`Commands` rows after configured launch payload parity and command prioritization.

Artifacts:

- HTML: `docs/superpowers/verification/p0.186-configured-acceptance-command-visual.html`
- PNG: `docs/superpowers/verification/assets/p0.186-configured-acceptance-command-visual-390x844.png`
- Viewport: `390x844`

The visual proof shows:

- `Final Launch Status`
- `Commands`
- `make final-acceptance-configured` as the first command row
- configured live consent copy
- `--allow-live-provider-calls`
- `commands_run=false live_calls=false global=false`

Safety:

- No provider calls were made.
- No configured final acceptance command was executed.
- No backend env, iOS deploy config, Xcode, signing, keychain, payment, or
  global machine state was modified.
- No provider secrets, raw media, local filesystem paths, private LAN URLs, or
  commerce links are present in the visual fixture.
