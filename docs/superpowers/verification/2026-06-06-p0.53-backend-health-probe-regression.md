# P0.53 Backend Health Probe Regression

Date: 2026-06-06

## Scope

P0.53 adds an operator-triggered iOS backend health probe. The app calls the
configured `PMFBackendBaseURL` `/health` endpoint once per tap and folds the
result into the `Device Preflight` backend row.

## Verification

```bash
make backend-lint
```

Result: passed.

```bash
make backend-test
```

Result: `186 passed, 1 warning`. The warning is the existing FastAPI/Starlette
`TestClient` deprecation warning for `httpx`.

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests
```

Result: passed.

```bash
swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks
```

Result: passed.

```bash
swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck
```

Result: passed.

```bash
cd services/backend
uv run python -m myth_forge_api.cli final-acceptance \
  --profile quick \
  --repo-root ../.. \
  --output /tmp/personal-myth-forge-final-acceptance-p0.53.json
```

Result: exit code `2`, expected for local blockers.

Summary:

```json
{
  "overall_status": "blocked",
  "summary": {
    "passed": 9,
    "blocked": 2,
    "failed": 0,
    "skipped": 0
  },
  "ios_showcase": {
    "passed": 14,
    "failed": 0
  }
}
```

Blocked checks:

- `mobile_deploy_preflight`: `blocked_by_local_ios_deploy_config`
- `mobile_xcode_build`: `blocked_by_apple_sdk_license`

```bash
git diff --check
```

Result: passed.

## Visual Evidence

Static mobile evidence:

```text
docs/superpowers/verification/p0.53-backend-health-probe.html
docs/superpowers/verification/assets/p0.53-backend-health-probe-390x844.png
```

The 390x844 screenshot was visually checked for mobile-width clipping and panel
readability.
