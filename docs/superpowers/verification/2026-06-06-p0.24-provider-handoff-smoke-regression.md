# P0.24 Provider Handoff Smoke Regression Evidence

Date: 2026-06-06

Scope:
- Add `provider-handoff` CLI command.
- Produce a non-secret provider handoff JSON report.
- Report core real readiness for `three_d`, `npc`, and `capture_storage`.
- Return exit code `2` when `--require-core-real` is requested and core real
  providers are not ready.
- Avoid live Meshy/OpenAI calls and global environment mutation.

## Baseline Evidence

Before implementation in the P0.24 worktree:

- `make backend-test`: 101 passed, 1 existing Starlette/httpx warning.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`: passed.

## TDD Evidence

CLI RED:
- `cd services/backend && uv run pytest tests/test_cli.py -q`
- Result: 4 failed, 4 passed.
- Failure: `argparse` rejected `provider-handoff` because the subcommand did not exist.

CLI GREEN:
- `cd services/backend && uv run pytest tests/test_cli.py -q`
- Result: 8 passed.

## Visual Evidence

Static evidence page:
- `docs/superpowers/verification/p0.24-provider-handoff-smoke.html`

Captured screenshot:
- `docs/superpowers/verification/assets/p0.24-provider-handoff-smoke-390x844.png`
- `file docs/superpowers/verification/assets/p0.24-provider-handoff-smoke-390x844.png`
- Result: PNG image data, 390 x 844, 8-bit/color RGB, non-interlaced.

Manual visual check:
- Shows `Provider Handoff Smoke`.
- Shows `core_real_ready`.
- Shows Meshy/OpenAI missing env names.
- Shows backend-only secret boundary.
- Shows next handoff commands.

## Full Worktree Verification

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks`

Result:
- Passed.

Command:
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests`

Result:
- Passed.

Command:
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck`

Result:
- Passed.

Command:
- `make backend-lint`

Result:
- `All checks passed!`

Command:
- `make backend-test`

Result:
- 105 passed, 1 existing Starlette/httpx deprecation warning.

Command:
- `make mobile-xcode-build`

Result:
- Stopped at the Apple SDK license gate:
  `You have not agreed to the Xcode license agreements.`
- `make: *** [mobile-xcode-build] Error 69`
- No `sudo xcodebuild -license` command was run.

Command:
- `git diff --check HEAD`

Result:
- Passed with no output.

## Safety Checks

- `provider-handoff` only reads settings and provider readiness.
- It does not instantiate live Meshy/OpenAI calls.
- It writes missing env names, not secret values.
- It identifies provider key variables as backend-only.
- It does not add iOS-side key handling.
