# P0.97 Mobile Final Showcase Launch Digest Visual Regression

## Artifact

- HTML: `docs/superpowers/verification/p0.97-mobile-final-showcase-launch-digest.html`
- PNG: `docs/superpowers/verification/assets/p0.97-mobile-final-showcase-launch-digest-390x844.png`
- Viewport: 390x844

## Result

The static iPhone visual shows the `Final Showcase` card with the original local
demo rows plus the new launch-facing rows:

- `NPC Eval`
- `Handoff`
- `Launch`

Manual inspection confirms the rows are readable at 390x844. Labels and details
do not overlap, the `CHECK` badge remains in the card header, and the following
`Demo Script` card remains visible below the top card.

## Chrome Capture Notes

Chrome wrote the PNG but did not exit on its own. The main Chrome process using
the temporary `/tmp/pmf-chrome-p0.97.*` profile was terminated after the PNG was
written. The temporary profile directory was removed, and no matching
`/tmp/pmf-chrome-p0.97.*` directory remained after capture.
