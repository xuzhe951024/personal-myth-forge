# P0.85 NPC Action Gate Regression

Date: 2026-06-06
Branch: `codex/p0.85-npc-action-gate`

## Scope

P0.85 connects the visible NPC Agent setup state to `Advance Village` and `Run Autonomy`. The NPC actions now follow a mobile-core `NPCAgentActionGate` instead of checking only session/loading booleans in the SwiftUI view.

## TDD Evidence

- Baseline before implementation:
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
  - `uv run pytest tests/test_ios_showcase_acceptance.py -q` passed.
- RED:
  - Added NPC action gate contract tests first.
  - `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` failed because `NPCAgentActionGateBuilder` was not defined.
- GREEN:
  - Added `NPCAgentActionGate` and `NPCAgentActionGateBuilder`.
  - Fixed one test assertion after root-cause check: existing running detail begins with `Running`, while the new test expected lowercase `running`.
  - Focused Swift core contract tests passed.
  - SwiftUI app compile gate passed.

## Verification

- `make backend-lint` passed.
- `make backend-test` passed: 267 passed, 1 existing FastAPI/Starlette `TestClient` deprecation warning.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileCoreContractTests` passed.
- `swift build --package-path apps/mobile/ios --product PersonalMythForgeMobileAppCompileCheck` passed.
- `swift run --package-path apps/mobile/ios PersonalMythForgeMobileProjectChecks` passed.
- `uv run pytest tests/test_ios_showcase_acceptance.py -q` passed.
- `uv run python -m myth_forge_api.cli final-acceptance --profile quick --repo-root ../..` exited 2 as expected with 12 passed, 2 blocked, 0 failed.

During verification, quick final acceptance caught one stale source-gate expectation that still looked for `Run Autonomy` inside `NPCTickView.swift`. P0.85 moves that label into `NPCAgentActionGate`, so the source-gate evidence was updated to `NPCAgentTickSummary.swift::autonomyTitle: "Run Autonomy"` and the focused iOS showcase acceptance test was rerun.

## Visual Evidence

- HTML source: `docs/superpowers/verification/p0.85-npc-action-gate.html`
- Screenshot: `docs/superpowers/verification/assets/p0.85-npc-action-gate-390x844.png`
- Visual QA result: the 390x844 image shows `NPC Agent Mode`, missing `OPENAI_API_KEY`, disabled `Advance Village`, disabled `Run Autonomy`, and visible action-gate detail without text overlap or right-edge clipping.

## Known Blockers

- `mobile_deploy_preflight` is expected to block until `Deployment.local.xcconfig` has a real `DEVELOPMENT_TEAM` and a non-loopback iPhone-reachable backend URL.
- `mobile_xcode_build` is expected to block until the local Apple SDK/Xcode license is accepted. P0.85 does not run global Xcode or signing commands.
