# P0.13 iOS App Compile Gate Visual Regression

Date: 2026-06-06

## Target

Static mobile storyboard:

```text
docs/superpowers/verification/p0.13-ios-app-compile-gate.html
```

Rendered through a local `127.0.0.1` static server at a `390x844` viewport.

## Evidence

Screenshot:

```text
docs/superpowers/verification/assets/p0.13-ios-app-compile-gate-390x844.png
```

## Browser Metrics

- Viewport: `390x844`
- Document width: `390`
- Document height: `844`
- Phone frame height: `834`
- Horizontal overflow: `false`
- Console errors: `[]`
- Compile command visible: `true`
- App source coverage visible: `true`
- Swift 6 concurrency / MainActor evidence visible: `true`
- No-simulator evidence notice visible: `true`
- No-simulator evidence notice fully in viewport: `true`
- Required text present: `App Compile Gate`, `Gate Coverage`, `Issues This Gate Caught`, `Sendable callback state mutation`

## Result

Pass. The storyboard exposes the P0.13 local deployability gate:

```text
SwiftPM app compile target -> App source compile -> SwiftUI availability checks -> Swift 6 concurrency checks
```

This is source compile gate evidence, not a live simulator run, device install,
ARKit mesh capture, signing pass, or iOS 3D renderer screenshot.
