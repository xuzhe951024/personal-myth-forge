# P0.90 NPC Agent Evaluation Visual Regression

## Commands

```bash
cd services/backend
uv run python -m myth_forge_api.cli evaluate-npc \
  --provider local \
  --suite default-v0 \
  --tick-steps 2 \
  --output .local/npc-evaluation-p0.90.json
```

Rendered with Playwright using the locally installed Google Chrome executable:

```text
docs/superpowers/verification/p0.90-npc-agent-evaluation.html
docs/superpowers/verification/assets/p0.90-npc-agent-evaluation-390x844.png
```

## Result

- Report kind: `npc_agent_evaluation_report`
- Suite: `default-v0`
- Provider: `local`
- Cases: `6 succeeded`, `0 failed`
- Coverage: `6 expected NPC sets`, `6 trace sets`, `6 proposed-action plan matches`, `12 tick steps`, `12 world-resolution steps`

## Visual Inspection

The 390x844 screenshot shows the NPC Agent Evaluation receipt, coverage, safety
rows, and first-case summary without overlapping text, clipped controls, local
paths, provider secrets, raw private markers, or media payloads.

No OpenAI, Meshy, Treatstock, Xcode, signing, deployment, network, or global
configuration command was executed for this visual artifact.
