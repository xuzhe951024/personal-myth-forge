# P0.153 Configured Evidence Visual Regression

P0.153 adds a default visual regression artifact for the iPhone Final Launch
`Configured Evidence` rows introduced by P0.151 and refined by P0.152.

Artifacts:

- HTML: `docs/superpowers/verification/p0.153-configured-evidence-visual.html`
- PNG: `docs/superpowers/verification/assets/p0.153-configured-evidence-visual-390x844.png`
- Viewport: `390x844`

The visual proof shows:

- `Configured Evidence`
- `Configured evidence blocked`
- `consent now 0, planned 3`
- live/cost/repo-write counts
- `--allow-live-provider-calls`
- `commands_run=false live_calls=false`

Safety:

- No provider calls were made.
- No configured evidence commands were executed.
- No backend `.env`, iOS deploy config, Xcode, signing, keychain, or global state
  was modified.
- No provider secrets, raw media, local filesystem paths, private LAN URLs, or
  commerce links are present in the visual fixture.
