# P0.221 Mobile Auto Backend URL Handoff Visual Regression

This static 390x844 iPhone evidence card covers the Final Launch Status panel after backend final resource reports expose `PMF_BACKEND_BASE_URL=auto`.

The card verifies that the mobile handoff can show `apply_time_auto`, the deployment writer, and `final-apply-resources` without exposing resolved local network addresses, provider secrets, payment links, raw media, Xcode actions, or global machine mutations.
