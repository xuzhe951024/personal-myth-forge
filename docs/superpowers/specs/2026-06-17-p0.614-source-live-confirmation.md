# P0.614 Source Live Confirmation

## Problem

The downstream final showcase, demo launch, closure, and external-action reports now preserve explicit live-provider confirmation metadata. The source reports that feed them still leave some live Meshy/OpenAI blocker rows with only `requires_live_provider_consent`, which forces downstream readers to infer cost and user-confirmation risk.

## Scope

- Add `requires_cost_consent`, `live_provider_call`, and `requires_user_confirmation` to live/cost rows in `final-configured-evidence-plan`.
- Preserve the same fields on `live-provider-evidence` first blockers and next actions for live Meshy/OpenAI evidence slots.
- Keep non-live local handoff rows unchanged except for existing metadata.
- Do not run live provider commands, external paid/provider actions, Xcode signing, or global writes.

## Acceptance

- Focused RED tests prove the source reports lack the explicit confirmation fields.
- GREEN implementation exposes the fields directly from the source reports.
- Focused and adjacent backend tests remain green.
- Safe `.local` report refresh shows the source reports carrying the same confirmation metadata as downstream final handoff reports.
